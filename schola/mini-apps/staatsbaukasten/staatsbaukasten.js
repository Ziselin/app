(() => {
  'use strict';
  const TYPES=[['institution','Institution','I','#65736e'],['group','Strukturgruppe','▣','#65736e'],['collection','Einfache Sammlung','≡','#65736e'],['collection-group','Sammlungsgruppe','▤','#65736e'],['constitution','Rechtsgrundlage','§','#65736e']];
  const LEGACY_TYPES=new Set(['people','electorate','assembly','parliament','head','government','administration','court','control','council','office','territory','other']);
  const normalizeElementType=type=>TYPES.some(item=>item[0]===type)?type:LEGACY_TYPES.has(type)?'institution':'institution';
  const TYPE_INFO={
    institution:['Ein frei benennbares staatliches oder politisches Einzelelement.','Nutze diesen universellen Baustein für Organe, Ämter, Personengruppen, Gebietskörperschaften und sonstige Institutionen. Die fachliche Rolle entsteht durch Bezeichnung, Ebene, Gewalt, Eigenschaften und Beziehungen.','Bundestag, Bundespräsident, Wahlberechtigte, Verfassungsgericht, Kommune'],
    group:['Ein sichtbarer Rahmen für mehrere eigenständige Diagrammelemente.','Ziehe Institutionen in die Strukturgruppe oder weise sie im Eigenschaftenbereich zu. Die enthaltenen Elemente behalten eigene Eigenschaften und Beziehungen; auch die Gruppe selbst kann verbunden werden.','Bundesregierung mit Bundeskanzler und Bundesministern, Parlament mit zwei Kammern'],
    'collection-group':['Verwaltet mehrere einfache Sammlungen und zeigt jeweils genau eine davon innerhalb ihres Rahmens.','Lege die Sammlungen über die Gruppenverwaltung an. Über die Auswahl im Diagramm wechselst du die sichtbare Sammlung; diese lässt sich anschließend wie jede einfache Sammlung bearbeiten.','Ministerien mit jeweils zugehörigen Behörden und Einrichtungen'],
    people:['Bezeichnet die Menschen, von denen politische Herrschaft ausgeht oder auf die sie sich bezieht.','Nutze diesen Typ für das Volk als Träger von Souveränität. Wahlberechtigte können davon getrennt als Wahlkörper modelliert werden.','Staatsvolk, Bürgerschaft, Untertanen eines historischen Gemeinwesens'],
    electorate:['Umfasst die Personen oder Gremien, die an einer bestimmten Wahl beteiligt sind.','Modelliere damit präzise, wer ein Organ wählt – besonders wenn Wahlkörper und Gesamtbevölkerung nicht identisch sind.','Wahlberechtigte, Wahlmännerkollegium, Fürstenkollegium'],
    assembly:['Ein aus mehreren Personen bestehendes Organ, das Gruppen oder Interessen vertritt.','Geeignet für Vertretungen, die nicht genauer als Parlament oder Kammer bestimmt werden sollen.','Nationalversammlung, Ständeversammlung, Volkskongress'],
    parliament:['Ein kollegiales Vertretungsorgan mit Aufgaben insbesondere in Gesetzgebung, Kontrolle und politischer Willensbildung.','Mehrere Kammern werden als getrennte Elemente angelegt und über ihre Beziehungen modelliert.','Bundestag, Senat, Unterhaus, Bundesrat'],
    head:['Das oberste repräsentative Amt eines Staates; seine politische Macht kann sehr unterschiedlich sein.','Eigenschaften wie Amtszeit oder monarchische Struktur gehören an das Element, Wahl oder Ernennung an die Beziehung.','Bundespräsident, Monarchin, Präsident'],
    government:['Das politische Leitungsorgan der Exekutive oder die Person an seiner Spitze.','Regierung und Regierungschef können gemeinsam oder als getrennte Elemente modelliert werden, wenn ihre Beziehungen wichtig sind.','Kabinett, Ministerrat, Bundeskanzler, Premierministerin'],
    administration:['Organe, die politische Entscheidungen vorbereiten, konkretisieren oder ausführen.','Nutze diesen Typ für nachgeordnete Exekutivstrukturen; Weisungs- und Aufsichtsverhältnisse werden als Beziehungen dargestellt.','Ministerium, Behörde, Ressortleitung, Präfektur'],
    court:['Ein unabhängiges oder institutionell abgegrenztes Organ zur Rechtsprechung und rechtlichen Prüfung.','Spezielle Zuständigkeiten wie Verfassungsprüfung werden als Eigenschaft oder Beziehung erfasst.','Verfassungsgericht, Supreme Court, Verwaltungsgericht'],
    control:['Ein spezialisiertes Organ für Aufsicht, Prüfung, Wahlen oder institutionelle Kontrolle.','Verwende es, wenn die besondere Kontroll- oder Verfahrensfunktion wichtiger ist als eine Zuordnung zu den drei Gewalten.','Rechnungshof, Wahlkommission, Ombudsstelle'],
    council:['Ein kollegiales Beratungs-, Entscheidungs- oder Koordinationsorgan.','Der Typ eignet sich für institutionell eigenständige Räte, die weder Parlament noch Regierung sind.','Staatsrat, Kronrat, Ältestenrat, Revolutionsrat'],
    office:['Ein einzelnes Amt oder Organ, das keiner spezielleren Kategorie entspricht.','Benenne die konkrete Funktion frei und bilde Besetzung, Unterstellung oder Verantwortung über Beziehungen ab.','Gouverneur, Reichskanzler, Hoher Kommissar'],
    territory:['Eine räumlich abgegrenzte politische Körperschaft oder staatliche Ebene.','Territoriale Zugehörigkeit, Autonomie und Kompetenzteilung werden als gerichtete Beziehungen modelliert.','Gliedstaat, Provinz, Region, Kommune'],
    other:['Eine Institution, die sich fachlich keiner vorhandenen Grundkategorie sinnvoll zuordnen lässt.','Nutze diesen offenen Typ sparsam und benenne das Element so präzise, dass seine institutionelle Rolle verständlich bleibt.','Historisches Sonderorgan, religiöse Autorität, informelles Machtzentrum']
  };
  const RELATIONS={
    'Besetzung und Legitimation':['wählt','ernennt','nominiert','schlägt vor','bestätigt','bestellt','entsendet','beruft'],
    'Kontrolle und Verantwortung':['kontrolliert','beaufsichtigt','prüft','genehmigt','muss berichten an','ist verantwortlich gegenüber','kann beanstanden'],
    'Amtsbeendigung':['kann abberufen','kann entlassen','kann abwählen','kann auflösen','kann Amtsenthebung einleiten','kann Amtsenthebung beschließen'],
    'Gesetzgebung':['besitzt Gesetzesinitiative gegenüber','beschließt','muss zustimmen','kann widersprechen','besitzt Vetorecht gegenüber','kann Veto überstimmen','fertigt aus','verkündet'],
    'Exekutive und Hierarchie':['leitet','weist an','untersteht','gehört an','führt aus','delegiert Kompetenz an','übt Kompetenz gemeinsam aus'],
    'Gerichtliche Beziehungen':['prüft Entscheidung von','kann Entscheidung aufheben','prüft Verfassungsmäßigkeit','ist Rechtsmittelinstanz für'],
    'Territoriale Beziehungen':['ist Teil von','besteht aus','ist vertreten in','entsendet in','besitzt Autonomie gegenüber','unterliegt Aufsicht durch','teilt Kompetenz mit']
  };
  const DEFAULT_BRANCHES=[{id:'legislative',label:'Legislative',color:'#b38a35',active:true},{id:'executive',label:'Exekutive',color:'#c76842',active:true},{id:'judiciary',label:'Judikative',color:'#557060',active:true}];
  const GRID=24,snapGrid=value=>Math.round(value/GRID)*GRID,ceilGrid=value=>Math.ceil(value/GRID)*GRID;
  const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)], uid=p=>`${p}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,6)}`;
  const blank=()=>({version:1,title:'Mein Staatsmodell',mode:'basic',elements:[],relations:[],groups:[],branches:DEFAULT_BRANCHES.map(branch=>({...branch})),view:{}});
  let model=load(), selected=null, pending=null, drag=null, guides=[], saveTimer,gestureSaveTimer,sourceDrafts=[],sourceDraftIndex=0,sourceInfoItems=[],sourceInfoIndex=0,collectionDrafts=[],collectionEditorMode='items',openCollectionId=null;
  function categoryForRelation(r){return r?.category&&RELATIONS[r.category]?r.category:(Object.entries(RELATIONS).find(([,types])=>types.includes(r?.type))?.[0]||Object.keys(RELATIONS)[0])}
  function activeRelationCategory(){const categories=Object.keys(RELATIONS),saved=model.view?.relationCategory;return categories.includes(saved)?saved:categories[0]}
  function visibleRelations(){const category=activeRelationCategory();return model.relations.filter(r=>(model.mode!=='advanced'||categoryForRelation(r)===category)&&nodeVisible(model.elements.find(n=>n.id===r.source))&&nodeVisible(model.elements.find(n=>n.id===r.target)))}
  function categoryForType(type,fallback=activeRelationCategory()){return Object.entries(RELATIONS).find(([,types])=>types.includes(type))?.[0]||fallback}
  const els={canvas:$('#canvas'),viewport:$('#canvasViewport'),nodes:$('#nodeLayer'),groups:$('#groupLayer'),filters:$('#levelFilters'),branches:$('#branchFilters'),edges:$('#edges'),guides:$('#guides'),paletteInfo:$('#paletteInspector'),nodeForm:$('#nodeInspector'),edgeForm:$('#edgeInspector'),groupForm:$('#groupInspector'),branchForm:$('#branchInspector')};
  function normalizedBranches(value){const input=Array.isArray(value)?value:[];return DEFAULT_BRANCHES.map(fallback=>{const branch=input.find(item=>item.id===fallback.id||item.label===fallback.label)||{};return{...fallback,color:/^#[0-9a-f]{6}$/i.test(branch.color||'')?branch.color:fallback.color,active:typeof branch.active==='boolean'?branch.active:true}})}
  function ensureBranches(){const valid=Array.isArray(model.branches)&&model.branches.length===DEFAULT_BRANCHES.length&&DEFAULT_BRANCHES.every(fallback=>model.branches.some(branch=>branch.id===fallback.id&&branch.label===fallback.label&&/^#[0-9a-f]{6}$/i.test(branch.color||'')&&typeof branch.active==='boolean'));if(!valid)model.branches=normalizedBranches(model.branches);return model.branches}
  function branchForNode(n){const name=n?.properties?.branch;if(!Array.isArray(model.branches))ensureBranches();return model.branches.find(branch=>branch.label===name)}
  function relationColor(r){const source=model.elements.find(n=>n.id===r?.source),branch=branchForNode(source);return branch?.active?branch.color:'#213633'}
  function load(){try{const data=JSON.parse(localStorage.getItem('schola-staatsbaukasten-v1'));if(data?.version!==1)return blank();data.elements=(data.elements||[]).map(node=>({...node,type:normalizeElementType(node.type)}));return data}catch{return blank()}}
  function save(){clearTimeout(saveTimer);remember();$('#saveState').textContent='Wird gespeichert …';saveTimer=setTimeout(()=>{try{localStorage.setItem('schola-staatsbaukasten-v1',JSON.stringify(model));$('#saveState').textContent='Lokal gespeichert'}catch{$('#saveState').textContent='Speicher voll – bitte exportieren'}},250)}
  function init(){
    document.body.classList.remove('diagram-mode');
    $('#elementPalette').innerHTML=TYPES.map(t=>`<button class="palette-item" draggable="true" data-type="${t[0]}" style="--type-color:${t[3]}"><span class="type-icon">${t[2]}</span><span>${t[1]}</span></button>`).join('');
    const options=TYPES.map(t=>`<option value="${t[0]}">${t[1]}</option>`).join('');els.nodeForm.elements.type.innerHTML=options;
    $('#relationLayerSelect').innerHTML=Object.keys(RELATIONS).map(c=>`<option>${c}</option>`).join('');$('#relationLayerSelect').value=activeRelationCategory();fillRelationTypes();
    bind();$('#modelTitle').value=model.title;setMode(model.mode,false);setZoom(model.view?.zoom||1,false);render();
  }
  function bind(){
    const menu=$('#sideMenu'),overlay=$('#menuOverlay'),menuButton=$('#menuButton');
    const setMenu=open=>{menu.classList.toggle('is-open',open);menu.setAttribute('aria-hidden',String(!open));menu.inert=!open;overlay.hidden=!open;menuButton?.setAttribute('aria-expanded',String(open))};
    if(menuButton)menuButton.onclick=()=>setMenu(true);$('#menuCloseButton').onclick=()=>setMenu(false);overlay.onclick=()=>setMenu(false);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu.classList.contains('is-open'))setMenu(false)});
    const canvasCenter=()=>{const r=els.canvas.getBoundingClientRect();return{clientX:r.left+r.width/2,clientY:r.top+r.height/2}};
    const finishGesture=()=>{clearTimeout(gestureSaveTimer);gestureSaveTimer=setTimeout(save,180)},trackpadWheel=e=>e.deltaMode===0&&(Math.abs(e.deltaX)>.01||Math.abs(e.deltaY)<90);
    $('#zoomOutButton').onclick=()=>setZoom(currentZoom()-.1,true,canvasCenter());$('#zoomInButton').onclick=()=>setZoom(currentZoom()+.1,true,canvasCenter());els.canvas.addEventListener('wheel',e=>{e.preventDefault();if(e.ctrlKey){setZoom(currentZoom()*Math.exp(-e.deltaY*.0012),false,e);finishGesture();return}if(trackpadWheel(e)){model.view=model.view||{};model.view.panX=(model.view.panX||0)-e.deltaX;model.view.panY=(model.view.panY||0)-e.deltaY;applyView();finishGesture();return}setZoom(currentZoom()+(e.deltaY<0?.1:-.1),true,e)},{passive:false});
    $$('.palette-item').forEach(b=>{b.addEventListener('click',()=>select({kind:'palette',id:b.dataset.type}));b.addEventListener('dragstart',e=>{e.dataTransfer.effectAllowed='copy';e.dataTransfer.setData('text/type',b.dataset.type)})});
    els.canvas.addEventListener('dragover',e=>{e.preventDefault();e.dataTransfer.dropEffect='copy'});els.canvas.addEventListener('drop',e=>{e.preventDefault();const type=e.dataTransfer.getData('text/type');if(type){const p=point(e),parentId=e.target.closest?.('.node')?.dataset.id||'';addNode(type,p.x,p.y,parentId)}});
    els.canvas.addEventListener('pointerdown',e=>{if(e.button===1){e.preventDefault();e.stopPropagation();drag={kind:'pan',startX:e.clientX,startY:e.clientY,originX:model.view?.panX||0,originY:model.view?.panY||0};els.canvas.classList.add('is-panning');els.canvas.setPointerCapture(e.pointerId);return}if(e.target===els.canvas||e.target===els.viewport){if(pending){pending=null;renderEdges()}select(null)}},true);els.canvas.addEventListener('pointermove',move);els.canvas.addEventListener('pointerup',endDrag);els.canvas.addEventListener('pointercancel',endDrag);els.canvas.addEventListener('auxclick',e=>{if(e.button===1)e.preventDefault()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&pending){pending=null;renderEdges()}});
    $$('.mode-switch button').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));$('#modelTitle').oninput=e=>{model.title=e.target.value;save()};
    $('#addGroupButton').onclick=()=>{model.groups.push({id:uid('group'),label:`Staatliche Ebene ${model.groups.length+1}`,kind:'level',active:true,x:0,y:0,w:1,h:1});select({kind:'group',id:model.groups.at(-1).id});render();save()};
    $('#relationLayerSelect').onchange=e=>{model.view=model.view||{};model.view.relationCategory=e.target.value;if(selected?.kind==='edge'&&categoryForRelation(getSelected())!==e.target.value)selected=null;pending=null;fillRelationTypes();renderEdges();renderInspector();save()};$('#relationType').onchange=()=>{$('#customRelationDialogField').hidden=$('#relationType').value!=='__custom__';if($('#relationType').value==='__custom__')$('#relationCustom').focus()};$('#confirmRelation').onclick=e=>{e.preventDefault();confirmRelation()};$('#editRelationSource').onclick=openRelationSource;$('#editNodeSource').onclick=openRelationSource;$('#saveRelationSource').onclick=e=>{e.preventDefault();saveRelationSource()};$('#sourcePrevious').onclick=()=>navigateSourceDraft(-1);$('#sourceNext').onclick=()=>navigateSourceDraft(1);$('#sourceAdd').onclick=addSourceDraft;$('#sourceInfoPrevious').onclick=()=>navigateSourceInfo(-1);$('#sourceInfoNext').onclick=()=>navigateSourceInfo(1);$('#editCollectionItems').onclick=openCollectionEditor;$('#collectionAdd').onclick=addCollectionDraft;$('#saveCollectionItems').onclick=e=>{e.preventDefault();saveCollectionItems()};
    $('#relationDialog').addEventListener('close',()=>{if(pending){pending=null;renderEdges()}});
    [els.nodeForm,els.edgeForm,els.groupForm,els.branchForm].forEach(f=>f.addEventListener('input',updateFromInspector));$$('[data-delete]').forEach(b=>b.onclick=()=>removeSelected());$('#resetBranchColor').onclick=()=>{if(selected?.kind!=='branch')return;const branch=getSelected(),fallback=DEFAULT_BRANCHES.find(item=>item.id===branch.id);branch.color=fallback.color;render();save()};
    els.branches.addEventListener('change',e=>{if(e.target.matches('input[type="checkbox"]'))renderEdges()});
    $$('[data-layout]').forEach(b=>b.onclick=()=>arrangeGroup(b.dataset.layout));
    const actionMenu=$('#canvasActionMenu'),closeActionMenu=()=>actionMenu.removeAttribute('open');actionMenu.addEventListener('click',e=>{if(e.target.closest('.canvas-action-popover button'))closeActionMenu()});document.addEventListener('pointerdown',e=>{if(actionMenu.open&&!actionMenu.contains(e.target))closeActionMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&actionMenu.open){closeActionMenu();actionMenu.querySelector('summary').focus()}});
    $('#newButton').onclick=()=>{if(confirm('Das aktuelle Modell löschen und neu beginnen?')){model=blank();selected=null;$('#modelTitle').value=model.title;setMode('basic',false);setZoom(1,false);render();save()}};
    $('#generateButton').onclick=()=>{$('#generatorState').value='';$('#generatedPrompt').value='';$('#generatedPromptBlock').hidden=true;$('#promptCopyStatus').textContent='';$('#generatorDialog').showModal();requestAnimationFrame(()=>$('#generatorState').focus())};$('#createPromptButton').onclick=createGenerationPrompt;$('#copyPromptButton').onclick=copyGenerationPrompt;$('#generatorState').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();createGenerationPrompt()}});
    $('#exportButton').onclick=openExport;$('#exportSvgButton').onclick=exportSvg;$('#printPdfButton').onclick=printPdf;$('#exportJsonButton').onclick=exportModel;$('#importButton').onclick=()=>$('#importChoiceDialog').showModal();$('#importFileChoice').onclick=()=>{$('#importChoiceDialog').close();$('#importInput').click()};$('#importCodeChoice').onclick=()=>{$('#importChoiceDialog').close();$('#jsonImportCode').value='';$('#jsonImportError').hidden=true;$('#jsonImportDialog').showModal();requestAnimationFrame(()=>$('#jsonImportCode').focus())};$('#confirmJsonImport').onclick=importModelCode;$('#importInput').onchange=importModel;
    $('#fitButton').onclick=fit;
    window.addEventListener('resize',renderEdges);document.addEventListener('keydown',e=>{if((e.key==='Delete'||e.key==='Backspace')&&selected&&!/INPUT|TEXTAREA|SELECT/.test(e.target.tagName))removeSelected()});
  }
  function addNode(type,x,y,parentId=''){const rect=els.canvas.getBoundingClientRect(),zoom=currentZoom(),panX=model.view?.panX||0,panY=model.view?.panY||0;const t=TYPES.find(t=>t[0]===type),node={id:uid('element'),type,label:t[1].split(' / ')[0],properties:{},visual:{x:snapGrid(x??Math.max(96,(rect.width/2-panX)/zoom+(Math.random()-.5)*120)),y:snapGrid(y??Math.max(72,(rect.height/2-panY)/zoom+(Math.random()-.5)*120))}};const parent=model.elements.find(item=>item.id===parentId),ordinaryParent=parent&&!['collection','collection-group'].includes(parent.type),ordinaryCollectionGroupChild=parent?.type==='collection-group'&&!['collection','collection-group'].includes(type);if(parentId&&(ordinaryParent||ordinaryCollectionGroupChild))node.parentId=parentId;model.elements.push(node);assignNodeToArea(node);selected={kind:'node',id:node.id};centerElementsView();render();save()}
  function currentZoom(){return model.view?.zoom||1}
  function contentBounds(){const elements=model.mode==='advanced'?model.elements:model.elements.filter(nodeLevelActive),boxes=elements.map(nodeBox);for(const r of visibleRelations())for(const p of r.visual?.manualRoute||[])boxes.push({l:p.x,t:p.y,r:p.x,b:p.y});if(!boxes.length)return null;return{l:Math.min(...boxes.map(b=>b.l)),t:Math.min(...boxes.map(b=>b.t)),r:Math.max(...boxes.map(b=>b.r)),b:Math.max(...boxes.map(b=>b.b))}}
  function shiftDiagram(dx,dy){if(Math.abs(dx)<.001&&Math.abs(dy)<.001)return;for(const n of model.elements)if(!n.parentId){n.visual.x+=dx;n.visual.y+=dy}for(const r of model.relations)if(r.visual?.manualRoute)r.visual.manualRoute=r.visual.manualRoute.map(p=>({x:p.x+dx,y:p.y+dy}))}
  function reframeCanvas(centerScreen=false){
    model.view=model.view||{};const zoom=currentZoom(),bounds=contentBounds(),padding=240,minW=els.canvas.clientWidth/zoom,minH=els.canvas.clientHeight/zoom;
    if(!bounds){model.view.worldWidth=Math.max(800,minW);model.view.worldHeight=Math.max(600,minH);if(centerScreen){model.view.panX=0;model.view.panY=0}applyView();return syncCanvasLayers()}
    const width=ceilGrid(Math.max(minW,bounds.r-bounds.l+padding*2)),height=ceilGrid(Math.max(minH,bounds.b-bounds.t+padding*2)),dx=snapGrid(width/2-(bounds.l+bounds.r)/2),dy=snapGrid(height/2-(bounds.t+bounds.b)/2);
    shiftDiagram(dx,dy);model.view.worldWidth=width;model.view.worldHeight=height;
    if(centerScreen){model.view.panX=els.canvas.clientWidth/2-width/2*zoom;model.view.panY=els.canvas.clientHeight/2-height/2*zoom}else{model.view.panX=(model.view.panX||0)-dx*zoom;model.view.panY=(model.view.panY||0)-dy*zoom}
    applyView();syncCanvasLayers()
  }
  function canvasExtent(){model.view=model.view||{};return{w:Number(model.view.worldWidth)||Math.max(800,els.canvas.clientWidth/currentZoom()),h:Number(model.view.worldHeight)||Math.max(600,els.canvas.clientHeight/currentZoom())}}
  function applyView(){const zoom=currentZoom(),panX=model.view?.panX||0,panY=model.view?.panY||0,extent=canvasExtent();els.viewport.style.width=`${extent.w}px`;els.viewport.style.height=`${extent.h}px`;els.viewport.style.transform=`translate(${panX}px,${panY}px) scale(${zoom})`;$('#zoomValue').value=`${Math.round(zoom*100)} %`;positionCollectionMenu()}
  function panViewBy(dx,dy){model.view=model.view||{};model.view.panX=(model.view.panX||0)+dx;model.view.panY=(model.view.panY||0)+dy;applyView();save()}
  function centerElementsView(){reframeCanvas(true)}
  function setZoom(value,persist=true,anchor=null){const oldZoom=currentZoom(),zoom=Math.max(.1,Math.min(2.5,Math.round(value*100)/100));model.view=model.view||{};if(anchor&&zoom===oldZoom){applyView();return}if(anchor){const r=els.canvas.getBoundingClientRect(),localX=anchor.clientX-r.left,localY=anchor.clientY-r.top,panX=model.view.panX||0,panY=model.view.panY||0,worldX=(localX-panX)/oldZoom,worldY=(localY-panY)/oldZoom;model.view.zoom=zoom;model.view.panX=localX-worldX*zoom;model.view.panY=localY-worldY*zoom;applyView()}else{model.view.zoom=zoom;reframeCanvas(true)}if(persist)save()}
  function point(e){const r=els.canvas.getBoundingClientRect(),zoom=currentZoom(),panX=model.view?.panX||0,panY=model.view?.panY||0;return{x:(e.clientX-r.left-panX)/zoom,y:(e.clientY-r.top-panY)/zoom}}
  function startNodeDrag(e,id){const n=model.elements.find(n=>n.id===id);if(n.parentId)return;const p=point(e);drag={kind:'node',id,dx:p.x-n.visual.x,dy:p.y-n.visual.y,startX:n.visual.x,startY:n.visual.y,startPointerX:p.x,startPointerY:p.y,moved:false,attached:model.relations.filter(r=>r.source===id||r.target===id).map(r=>({id:r.id,end:r.source===id?'source':'target',points:routeRelation(r)})).filter(x=>x.points.length>=3)};e.currentTarget.setPointerCapture(e.pointerId)}
  function startConnect(e,id,chosenPort){e.preventDefault();e.stopPropagation();const p=point(e),node=model.elements.find(n=>n.id===id),box=connectionBox(node),port=chosenPort||DiagramGeometry.snapPort(box,DiagramGeometry.nearestPort(box,p)),at=DiagramGeometry.anchor(box,port);if(pending){if(id!==pending.source){pending.target=id;pending.targetPort=port;pending.x=at.x;pending.y=at.y;openRelation()}else{pending=null;renderEdges()}return}pending={source:id,sourcePort:port,startX:at.x,startY:at.y,x:at.x,y:at.y};renderEdges()}
  function startNodeInteraction(e,id){if(e.button!==0)return;if(selected?.kind==='node'&&selected.id===id&&hoverPort?.id===id)startConnect(e,id,hoverPort.port);else{if(pending){pending=null;renderEdges()}startNodeDrag(e,id)}}
  function move(e){if(drag?.kind==='corner'){const r=model.relations.find(x=>x.id===drag.id),raw=DiagramGeometry.magneticPoint(point(e),[drag.points[drag.index-1],drag.points[drag.index+1]],currentZoom()),targetPort=relationPorts(r).target,targetBox=nodeBox(model.elements.find(n=>n.id===r.target)),targetAnchor=DiagramGeometry.anchor(targetBox,targetPort),normal={left:{x:-1,y:0},right:{x:1,y:0},top:{x:0,y:-1},bottom:{x:0,y:1}}[targetPort.side],distance=Math.max(DiagramGeometry.TARGET_LEAD,(raw.x-targetAnchor.x)*normal.x+(raw.y-targetAnchor.y)*normal.y),p=drag.terminal?{x:targetAnchor.x+normal.x*distance,y:targetAnchor.y+normal.y*distance}:raw;r.visual=r.visual||{};r.visual.manualRoute=drag.points.map((v,i)=>i===drag.index?p:{...v});r.visual.freeRoute=true;drag.invalid=relationBlocked(r,routeRelation(r));applyView();renderEdges();return}if(drag?.kind==='segment'){const p=point(e),r=model.relations.find(x=>x.id===drag.id);r.visual=r.visual||{};r.visual.manualRoute=DiagramGeometry.moveSegment(drag.points,drag.index,p[drag.axis]-drag.start);r.visual.freeRoute=true;drag.invalid=relationBlocked(r,routeRelation(r));applyView();renderEdges();return}if(drag?.kind==='endpoint'){const r=model.relations.find(x=>x.id===drag.id),n=model.elements.find(x=>x.id===r[drag.end]);r.visual=r.visual||{};r.visual[drag.end+'Port']=DiagramGeometry.snapPort(nodeBox(n),DiagramGeometry.nearestPort(nodeBox(n),point(e)));renderEdges();return}if(!drag){const p=point(e),previous=hoverPort;hoverPort=findHoverPort(p);hoverNodeId=hoverPort?.id||null;if(pending){const at=hoverPort?.point||p;pending.x=at.x;pending.y=at.y;renderEdges()}else if(previous?.id!==hoverPort?.id||previous?.port.side!==hoverPort?.port.side||previous?.port.t!==hoverPort?.port.t)renderEndpointHandles();return}if(drag.kind==='pan'){model.view.panX=drag.originX+e.clientX-drag.startX;model.view.panY=drag.originY+e.clientY-drag.startY;applyView();return}if(drag.kind==='edge-label'){const r=model.relations.find(x=>x.id===drag.id);r.visual=r.visual||{};r.visual.labelPosition=Math.max(.02,Math.min(.98,DiagramGeometry.project(routeRelation(r),point(e))));const p=pointOnRoute(routeRelation(r),r.visual.labelPosition),label=[...$$('.edge-label')].find(x=>x.dataset.id===r.id);if(label){label.setAttribute('x',p.x);label.setAttribute('y',p.y-7)}els.edgeForm.elements.labelPosition.value=r.visual.labelPosition;return}const p=point(e);if(drag.kind==='node'){if(Math.hypot(p.x-drag.startPointerX,p.y-drag.startPointerY)>3/currentZoom())drag.moved=true;const n=model.elements.find(n=>n.id===drag.id),snapped=snapPosition(n,Math.max(75,p.x-drag.dx),Math.max(35,p.y-drag.dy));n.visual.x=snapped.x;n.visual.y=snapped.y;for(const attached of drag.attached||[]){const r=model.relations.find(r=>r.id===attached.id);if(!r)continue;r.visual=r.visual||{};r.visual.manualRoute=DiagramGeometry.moveAttached(attached.points,attached.end,n.visual.x-drag.startX,n.visual.y-drag.startY);r.visual.freeRoute=true}updateNodePositions();applyView();renderEdges()}else if(drag.kind==='group'){const g=model.groups.find(g=>g.id===drag.id),nx=Math.max(0,p.x-drag.dx),ny=Math.max(0,p.y-drag.dy),shiftX=nx-drag.lastX,shiftY=ny-drag.lastY;g.x=nx;g.y=ny;drag.lastX=nx;drag.lastY=ny;const ids=descendantGroupIds(g.id);model.groups.filter(x=>x.id!==g.id&&ids.includes(x.id)).forEach(x=>{x.x+=shiftX;x.y+=shiftY});model.elements.filter(n=>!n.parentId&&ids.includes(n.groupId)).forEach(n=>{n.visual.x+=shiftX;n.visual.y+=shiftY});updateGroupPositions();updateNodePositions();applyView();renderEdges()}else if(drag.kind==='group-resize'){const g=model.groups.find(g=>g.id===drag.id);g.w=Math.max(180,p.x-g.x);g.h=Math.max(100,p.y-g.y);updateGroupPositions();applyView()}}
  function endDrag(){if(!drag)return;const finished=drag,r=model.relations.find(r=>r.id===finished.id);if(finished.invalid&&finished.originalVisual){if(r)r.visual=finished.originalVisual}else if(finished.kind==='corner'||finished.kind==='segment'||finished.kind==='endpoint'){if(r)normalizeRelation(r);if(preservedPointRelationId===finished.id){preservedPointRelationId=null;preservedPoint=null}}if(finished.kind==='pan')els.canvas.classList.remove('is-panning');if(finished.kind==='node'){const n=model.elements.find(n=>n.id===finished.id);if(n)assignNodeToArea(n);for(const attached of finished.attached||[]){const relation=model.relations.find(r=>r.id===attached.id);if(relation)normalizeRelation(relation)}if(finished.moved){suppressNodeClickId=finished.id;setTimeout(()=>{if(suppressNodeClickId===finished.id)suppressNodeClickId=null},0)}}if(finished.kind!=='pan'&&finished.kind!=='edge-label')reframeCanvas(false);guides=[];renderGuides();drag=null;renderEdges();renderInspector();save()}
  function openRelation(){const s=model.elements.find(n=>n.id===pending.source),t=model.elements.find(n=>n.id===pending.target);fillRelationTypes();$('#relationCustom').value='';$('#customRelationDialogField').hidden=true;$('#relationSentence').textContent=`${s.label} → … → ${t.label}`;$('#relationDialog').showModal()}
  function selectableRelationTypes(){return model.mode==='advanced'?(RELATIONS[activeRelationCategory()]||[]):[...new Set(Object.values(RELATIONS).flat())]}
  function fillRelationTypes(){const list=selectableRelationTypes(),options=list.map(r=>`<option>${r}</option>`).join('')+'<option value="__custom__">Eigene Beziehung …</option>';$('#relationType').innerHTML=options;els.edgeForm.elements.relation.innerHTML=options}
  function confirmRelation(){if(!pending?.target)return;const custom=$('#relationType').value==='__custom__',type=(custom?$('#relationCustom').value:$('#relationType').value).trim();if(!type){$('#relationCustom').setCustomValidity('Bitte beschreibe die Beziehung.');$('#relationCustom').reportValidity();return}$('#relationCustom').setCustomValidity('');model.relations.push({id:uid('relation'),source:pending.source,target:pending.target,type,category:custom?activeRelationCategory():categoryForType(type),properties:{},visual:{sourcePort:pending.sourcePort,targetPort:pending.targetPort}});selected={kind:'edge',id:model.relations.at(-1).id};pending=null;$('#relationDialog').close();render();save()}
  const blankSource=()=>({law:'',citation:'',url:'',note:''});
  function normalizeSourceItem(value){return value&&typeof value==='object'?{law:String(value.law||''),citation:String(value.citation||''),url:String(value.url||''),note:String(value.note||'')}:{law:'',citation:'',url:'',note:typeof value==='string'?value:''}}
  function sourceList(r){const properties=r?.properties||{};if(Array.isArray(properties.sources)&&properties.sources.length)return properties.sources.map(normalizeSourceItem);const legacy=properties.sourceDetails&&typeof properties.sourceDetails==='object'?properties.sourceDetails:properties.source;return legacy?[normalizeSourceItem(legacy)]:[]}
  function sourceData(r){return sourceList(r)[0]||blankSource()}
  function normalizedSourceUrl(value){if(!value.trim())return'';try{const url=new URL(/^https?:\/\//i.test(value)?value:'https://'+value);return ['http:','https:'].includes(url.protocol)?url.href:''}catch{return''}}
  function hasSource(r){return sourceList(r).some(source=>source.law.trim()||source.citation.trim()||source.url.trim()||source.note.trim())}
  function captureSourceDraft(){if(!sourceDrafts.length)return;sourceDrafts[sourceDraftIndex]={law:$('#sourceLaw').value,citation:$('#sourceCitation').value,url:$('#sourceUrl').value,note:$('#sourceNote').value}}
  function showSourceDraft(){const source=sourceDrafts[sourceDraftIndex]||blankSource();$('#sourceLaw').value=source.law;$('#sourceCitation').value=source.citation;$('#sourceUrl').value=source.url;$('#sourceUrl').setCustomValidity('');$('#sourceNote').value=source.note;$('#sourcePosition').textContent=`${sourceDraftIndex+1} / ${sourceDrafts.length}`;$('#sourcePrevious').disabled=sourceDraftIndex===0;$('#sourceNext').disabled=sourceDraftIndex===sourceDrafts.length-1}
  function navigateSourceDraft(delta){captureSourceDraft();sourceDraftIndex=Math.max(0,Math.min(sourceDrafts.length-1,sourceDraftIndex+delta));showSourceDraft()}
  function addSourceDraft(){captureSourceDraft();sourceDrafts.push(blankSource());sourceDraftIndex=sourceDrafts.length-1;showSourceDraft();$('#sourceLaw').focus()}
  function openRelationSource(){if(!['edge','node'].includes(selected?.kind))return;const isNode=selected.kind==='node';$('#sourceEditorEyebrow').textContent=isNode?'Funktionsgrundlage':'Rechtsgrundlage';$('#sourceEditorTitle').textContent=isNode?'Quellen zur Funktion':'Quellen der Beziehung';sourceDrafts=sourceList(getSelected());if(!sourceDrafts.length)sourceDrafts=[blankSource()];sourceDraftIndex=0;showSourceDraft();$('#sourceDialog').showModal()}
  function saveRelationSource(){if(!['edge','node'].includes(selected?.kind))return $('#sourceDialog').close();captureSourceDraft();const sources=[];for(let i=0;i<sourceDrafts.length;i++){const draft=sourceDrafts[i],rawUrl=draft.url.trim(),url=normalizedSourceUrl(rawUrl);if(rawUrl&&!url){sourceDraftIndex=i;showSourceDraft();$('#sourceUrl').setCustomValidity('Bitte gib eine gültige Webadresse ein.');$('#sourceUrl').reportValidity();return}const source={law:draft.law.trim(),citation:draft.citation.trim(),url,note:draft.note.trim()};if(source.law||source.citation||source.url||source.note)sources.push(source)}const owner=getSelected();owner.properties=owner.properties||{};if(sources.length){owner.properties.sources=sources;owner.properties.sourceDetails=sources[0];owner.properties.source=sources.map(source=>[source.law,source.citation,source.note].filter(Boolean).join(': ')).filter(Boolean).join(' | ')||'Quelle'}else{delete owner.properties.sources;delete owner.properties.source;delete owner.properties.sourceDetails}$('#sourceDialog').close();renderNodes();renderEdges();renderInspector();save()}
  function showSourceInfoItem(){const source=sourceInfoItems[sourceInfoIndex]||blankSource(),url=normalizedSourceUrl(source.url);$('#sourceInfoLaw').textContent=source.law||'Rechtsgrundlage';$('#sourceInfoCitation').textContent=source.citation;$('#sourceInfoCitationBlock').hidden=!source.citation;$('#sourceInfoNote').textContent=source.note;$('#sourceInfoNoteBlock').hidden=!source.note;$('#sourceInfoUrl').href=url;$('#sourceInfoUrlBlock').hidden=!url;$('#sourceInfoPosition').textContent=`${sourceInfoIndex+1} / ${sourceInfoItems.length}`;$('#sourceInfoPrevious').disabled=sourceInfoIndex===0;$('#sourceInfoNext').disabled=sourceInfoIndex===sourceInfoItems.length-1}
  function navigateSourceInfo(delta){sourceInfoIndex=Math.max(0,Math.min(sourceInfoItems.length-1,sourceInfoIndex+delta));showSourceInfoItem()}
  function showSourceInfo(id){const owner=model.relations.find(item=>item.id===id)||model.elements.find(item=>item.id===id);if(!owner||!hasSource(owner))return;$('.source-info-dialog .eyebrow').textContent=model.elements.includes(owner)?'Quellen zur Funktion':'Rechtsgrundlagen der Beziehung';sourceInfoItems=sourceList(owner).filter(source=>source.law.trim()||source.citation.trim()||source.url.trim()||source.note.trim());sourceInfoIndex=0;showSourceInfoItem();$('#sourceInfoDialog').showModal()}
  function render(){renderGroups();renderNodes();renderEdges();renderInspector();$('#nodeCount').textContent=model.elements.length;$('#edgeCount').textContent=model.relations.length}
  function allChildrenOf(id){return model.elements.filter(n=>n.parentId===id)}
  function childrenOf(id){const parent=model.elements.find(n=>n.id===id),children=allChildrenOf(id);if(parent?.type!=='collection-group')return children;const collections=children.filter(n=>n.type==='collection'),active=collections.some(n=>n.id===parent.properties?.activeCollectionId)?parent.properties.activeCollectionId:collections[0]?.id;return children.filter(n=>n.type!=='collection'||n.id===active)}
  function collectionEntries(n){const value=n?.properties?.items,input=Array.isArray(value)?value:String(value||'').split(/\r?\n/);return input.map(item=>typeof item==='object'&&item?{label:String(item.label||item.name||'').trim(),short:String(item.short||item.abbreviation||'').trim()}:{label:String(item).trim(),short:''}).filter(item=>item.label).slice(0,40)}
  function collectionItems(n){return collectionEntries(n).map(item=>item.label)}
  function readCollectionDrafts(){collectionDrafts=$$('#collectionEditorList .collection-editor-row').map((row,index)=>({id:collectionDrafts[index]?.id||'',label:row.querySelector('[data-field="label"]').value,short:row.querySelector('[data-field="short"]').value}))}
  function renderCollectionDrafts(focusIndex=-1){const list=$('#collectionEditorList'),empty=$('#collectionEditorEmpty');list.innerHTML=collectionDrafts.map((draft,index)=>`<div class="collection-editor-row" role="listitem" data-index="${index}"><span class="collection-row-number" aria-hidden="true">${index+1}</span><label><span>Bezeichnung</span><input data-field="label" maxlength="180" value="${esc(draft.label)}" placeholder="${collectionEditorMode==='groups'?'z. B. Bundesministerien':'z. B. Bundesministerium des Innern'}"></label><label class="collection-short-field"><span>Kurzangabe <small>optional</small></span><input data-field="short" maxlength="60" value="${esc(draft.short||'')}" placeholder="z. B. BMI"></label><div class="collection-row-actions"><button type="button" class="quiet" data-move="-1" aria-label="Eintrag nach oben" ${index===0?'disabled':''}>↑</button><button type="button" class="quiet" data-move="1" aria-label="Eintrag nach unten" ${index===collectionDrafts.length-1?'disabled':''}>↓</button><button type="button" class="quiet collection-row-delete" data-remove aria-label="Eintrag löschen">×</button></div></div>`).join('');empty.hidden=!!collectionDrafts.length;$('#collectionAdd').disabled=collectionDrafts.length>=40;list.querySelectorAll('button').forEach(button=>button.onclick=()=>{readCollectionDrafts();const index=Number(button.closest('.collection-editor-row').dataset.index);if(button.dataset.move){const target=index+Number(button.dataset.move);[collectionDrafts[index],collectionDrafts[target]]=[collectionDrafts[target],collectionDrafts[index]];renderCollectionDrafts(target)}else{collectionDrafts.splice(index,1);renderCollectionDrafts(Math.min(index,collectionDrafts.length-1))}});if(focusIndex>=0)requestAnimationFrame(()=>list.querySelector(`.collection-editor-row[data-index="${focusIndex}"] input`)?.focus())}
  function openCollectionEditor(){if(selected?.kind!=='node')return;const node=getSelected();if(!['collection','collection-group'].includes(node.type))return;collectionEditorMode=node.type==='collection-group'?'groups':'items';collectionDrafts=collectionEditorMode==='groups'?allChildrenOf(node.id).filter(n=>n.type==='collection').map(n=>({id:n.id,label:n.label,short:String(n.properties?.term||'')})):collectionEntries(node);$('#collectionEditorTitle').textContent=collectionEditorMode==='groups'?'Sammlungen der Gruppe':'Institutionen der Sammlung';$('#collectionEditorHint').textContent=collectionEditorMode==='groups'?'Jeder Eintrag ist eine auswählbare Sammlung.':'Die Einträge erscheinen im aufklappbaren Sammlungsmenü.';renderCollectionDrafts();$('#collectionEditorDialog').showModal();requestAnimationFrame(()=>$('#collectionEditorList input')?.focus())}
  function openCollectionGroupEditor(){openCollectionEditor()}
  function addCollectionDraft(){readCollectionDrafts();if(collectionDrafts.length>=40)return;collectionDrafts.push({id:'',label:'',short:''});renderCollectionDrafts(collectionDrafts.length-1)}
  function saveCollectionItems(){if(selected?.kind!=='node')return $('#collectionEditorDialog').close();readCollectionDrafts();collectionDrafts=collectionDrafts.map(draft=>({...draft,label:draft.label.trim(),short:draft.short.trim()})).filter(draft=>draft.label).slice(0,40);const node=getSelected();if(collectionEditorMode==='items'){node.properties=node.properties||{};node.properties.items=collectionDrafts.map(({label,short})=>short?{label,short}:label);$('#collectionEditorDialog').close();renderNodes();renderInspector();save();return}const kept=new Set;for(const draft of collectionDrafts){let child=model.elements.find(n=>n.id===draft.id&&n.parentId===node.id);if(!child){child={id:uid('element'),type:'collection',label:'Neue Sammlung',parentId:node.id,properties:{items:[]},visual:{x:0,y:0}};model.elements.push(child)}child.label=draft.label;child.properties=child.properties||{};if(draft.short)child.properties.term=draft.short;else delete child.properties.term;kept.add(child.id);draft.id=child.id}const removed=allChildrenOf(node.id).filter(n=>n.type==='collection'&&!kept.has(n.id)).map(n=>n.id);model.elements=model.elements.filter(n=>!removed.includes(n.id));model.relations=model.relations.filter(r=>!removed.includes(r.source)&&!removed.includes(r.target));node.properties=node.properties||{};if(collectionDrafts.length&&!kept.has(node.properties.activeCollectionId))node.properties.activeCollectionId=collectionDrafts[0].id;if(!collectionDrafts.length)delete node.properties.activeCollectionId;$('#collectionEditorDialog').close();render();save()}
  function childGap(n){const ids=new Set(childrenOf(n.id).map(c=>c.id)),internal=model.relations.some(r=>ids.has(r.source)&&ids.has(r.target));return internal?48:24}
  function nodeSize(n){const children=childrenOf(n.id),collectionGroup=n.type==='collection-group',rawWidth=Math.max(n.type==='collection'||collectionGroup?240:168,Number(n.visual.width)||192,...children.map(c=>nodeSize(c).w+24)),width=ceilGrid(rawWidth),lines=wrapLabel(n.label,n.type==='collection'||collectionGroup?width-72:width-24),term=n.properties?.term?.trim(),contentHeight=lines.length*19+(term?20:0)+24,ownH=ceilGrid(Math.max(Number(n.visual.height)||72,contentHeight)),childHeight=children.length?children.reduce((sum,c)=>sum+nodeSize(c).h+childGap(n),0)+24:collectionGroup?96:0,h=ceilGrid(ownH+childHeight);return{w:width,ownH,h}}
  function nodePosition(n){if(!n.parentId)return{x:n.visual.x,y:n.visual.y};const parent=model.elements.find(p=>p.id===n.parentId);if(!parent)return{x:n.visual.x,y:n.visual.y};const pp=nodePosition(parent),ps=nodeSize(parent),siblings=childrenOf(parent.id),index=siblings.findIndex(c=>c.id===n.id),head=ps.ownH,gap=childGap(parent);return{x:snapGrid(pp.x),y:snapGrid(pp.y-ps.h/2+head+24+siblings.slice(0,index).reduce((sum,c)=>sum+nodeSize(c).h+gap,0)+nodeSize(n).h/2)}}
  function assignNodeToArea(n){}
  function snapPosition(n,x,y){x=snapGrid(x);y=snapGrid(y);const threshold=8/currentZoom(),others=model.elements.filter(o=>o.id!==n.id&&!o.parentId),xs=others.map(o=>nodePosition(o).x),ys=others.map(o=>nodePosition(o).y),nearX=xs.reduce((best,v)=>Math.abs(v-x)<Math.abs(best-x)?v:best,Infinity),nearY=ys.reduce((best,v)=>Math.abs(v-y)<Math.abs(best-y)?v:best,Infinity);guides=[];if(Number.isFinite(nearX)&&Math.abs(nearX-x)<=threshold){x=nearX;guides.push({axis:'x',value:x})}if(Number.isFinite(nearY)&&Math.abs(nearY-y)<=threshold){y=nearY;guides.push({axis:'y',value:y})}renderGuides();return{x,y}}
  function renderGuides(){const extent=canvasExtent();els.guides.innerHTML=guides.map(g=>g.axis==='x'?`<path class="alignment-guide" d="M ${g.value} 0 V ${extent.h}"/>`:`<path class="alignment-guide" d="M 0 ${g.value} H ${extent.w}"/>`).join('')}
  function descendantGroupIds(id){const ids=[id];model.groups.filter(g=>g.parentGroupId===id).forEach(g=>ids.push(...descendantGroupIds(g.id)));return ids}
  function arrangeGroup(mode){if(selected?.kind!=='group')return;const g=model.groups.find(x=>x.id===selected.id),nodes=model.elements.filter(n=>!n.parentId&&n.groupId===g.id);if(!g||!nodes.length)return;const cols=mode==='vertical'?1:mode==='horizontal'?nodes.length:Math.max(1,Math.ceil(Math.sqrt(nodes.length))),rows=Math.ceil(nodes.length/cols),cw=Math.max(...nodes.map(n=>nodeSize(n).w))+44,ch=Math.max(...nodes.map(n=>nodeSize(n).h))+56;g.w=Math.max(g.w,cols*cw+32);g.h=Math.max(g.h,rows*ch+48);nodes.forEach((n,i)=>{n.visual.x=g.x+16+cw*(i%cols+.5);n.visual.y=g.y+40+ch*(Math.floor(i/cols)+.5)});render();save()}
  function nodeLevelId(n){let current=n,guard=0;while(current&&guard++<50){if(current.groupId)return current.groupId;current=model.elements.find(x=>x.id===current.parentId)}return''}
  function nodeLevelActive(n){if(!n)return false;const id=nodeLevelId(n),level=model.groups.find(g=>g.id===id);return !level||level.active!==false}
  function nodeVisible(n){if(!n||(model.mode==='advanced'?false:!nodeLevelActive(n)))return false;const parent=model.elements.find(item=>item.id===n.parentId);if(parent?.type==='collection-group'&&n.type==='collection'){const collections=allChildrenOf(parent.id).filter(item=>item.type==='collection'),active=collections.some(item=>item.id===parent.properties?.activeCollectionId)?parent.properties.activeCollectionId:collections[0]?.id;if(n.id!==active)return false}return !parent||nodeVisible(parent)}
  function positionCollectionMenu(){const menu=$('#collectionMenu');if(!menu||menu.hidden)return;const node=els.nodes.querySelector(`.node[data-id="${openCollectionId}"]`);if(!node){menu.hidden=true;return}const canvasRect=els.canvas.getBoundingClientRect(),nodeRect=node.getBoundingClientRect(),margin=10,width=Math.min(320,Math.max(260,canvasRect.width-24)),maxHeight=Math.min(280,canvasRect.height-24),below=canvasRect.bottom-nodeRect.bottom-margin,above=nodeRect.top-canvasRect.top-margin,useBelow=below>=Math.min(180,maxHeight)||below>=above,x=Math.max(12,Math.min(canvasRect.width-width-12,nodeRect.left-canvasRect.left+(nodeRect.width-width)/2)),available=Math.max(96,Math.min(maxHeight,useBelow?below:above));menu.style.width=`${width}px`;menu.style.maxHeight=`${available}px`;menu.style.left=`${x}px`;menu.style.top=`${useBelow?nodeRect.bottom-canvasRect.top+margin:nodeRect.top-canvasRect.top-margin-available}px`;menu.classList.toggle('opens-above',!useBelow)}
  function renderCollectionMenu(){const menu=$('#collectionMenu'),n=model.elements.find(n=>n.id===openCollectionId);if(!menu||!n||!nodeVisible(n)){if(menu)menu.hidden=true;if(openCollectionId&&!n)openCollectionId=null;return}const groupMode=n.type==='collection-group',entries=groupMode?allChildrenOf(n.id).filter(child=>child.type==='collection').map(child=>({id:child.id,label:child.label,short:String(child.properties?.term||'')})):collectionEntries(n).map((item,index)=>({...item,id:`${n.id}-item-${index}`}));if(!entries.length){menu.hidden=true;return}menu.innerHTML=`<header><span>${groupMode?'Sammlung auswählen':'Enthaltene Institutionen'}</span><strong>${entries.length}</strong></header><ul>${entries.map(item=>`<li data-item-key="${item.id}" ${groupMode?'tabindex="0" role="button"':''}><span>${esc(item.label)}</span>${item.short?`<small>${esc(item.short)}</small>`:''}</li>`).join('')}</ul>`;menu.hidden=false;if(groupMode)menu.querySelectorAll('li').forEach(item=>{const choose=()=>{n.properties=n.properties||{};n.properties.activeCollectionId=item.dataset.itemKey;openCollectionId=null;renderNodes();renderEdges();save()};item.onclick=choose;item.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();choose()}}});menu.onwheel=e=>e.stopPropagation();menu.onpointerdown=e=>e.stopPropagation();positionCollectionMenu()}
  function renderCollectionGroupControls(){for(const group of model.elements.filter(n=>n.type==='collection-group'&&nodeVisible(n))){const node=els.nodes.querySelector(`.node[data-id="${group.id}"]`),children=allChildrenOf(group.id).filter(n=>n.type==='collection');if(!node)continue;node.classList.add('has-children','is-collection-group');const heading=node.querySelector('.node-heading');if(heading){heading.className='node-collection-heading';const toggle=document.createElement('button');toggle.type='button';toggle.className='collection-toggle collection-group-toggle';toggle.dataset.id=group.id;toggle.disabled=!children.length;toggle.setAttribute('aria-expanded',String(openCollectionId===group.id));toggle.setAttribute('aria-controls','collectionMenu');toggle.setAttribute('aria-label',children.length?`${children.length} Sammlungen in ${group.label} anzeigen`:`Noch keine Sammlung in ${group.label}`);toggle.textContent=children.length;heading.append(toggle)}if(!children.length){const empty=document.createElement('div');empty.className='collection-group-empty';empty.innerHTML='<span>Noch keine Sammlung</span>';node.append(empty)}}}
  function renderNodes(){els.nodes.innerHTML=[...model.elements].filter(nodeVisible).sort((a,b)=>nodeDepth(a)-nodeDepth(b)).map(n=>{const t=TYPES.find(t=>t[0]===n.type)||TYPES.at(-1),branch=branchForNode(n),p=nodePosition(n),size=nodeSize(n),children=childrenOf(n.id),items=collectionItems(n),expanded=openCollectionId===n.id,muted=model.mode==='advanced'&&!nodeLevelActive(n),term=n.properties?.term?.trim(),termLine=term?`<span class="node-term">${esc(term)}</span>`:'',sourceMark=hasSource(n)?`<button type="button" class="node-source-mark" data-id="${n.id}" aria-label="Quellen zur Funktion von ${esc(n.label)} anzeigen">§</button>`:'',toggle=n.type==='collection'&&items.length?`<button type="button" class="collection-toggle" data-id="${n.id}" aria-expanded="${expanded}" aria-controls="collectionMenu" aria-label="${items.length} Institutionen in ${esc(n.label)} ${expanded?'einklappen':'anzeigen'}">${items.length}</button>`:'',title=`<strong class="node-label">${wrapLabel(n.label,n.type==='collection'?size.w-68:size.w-24).map(esc).join('<br>')}</strong>${termLine}`,heading=n.type==='collection'?`<div class="node-collection-heading"><div class="node-title-copy">${title}</div>${toggle}</div>`:`<div class="node-heading">${title}</div>`;return `<article class="node ${selected?.kind==='node'&&selected.id===n.id?'selected':''} ${n.parentId?'is-child':''} ${children.length?'has-children':''} ${n.type==='collection'?'is-collection':''} ${branch&&branch.active?'has-branch':''} ${branch&&!branch.active?'branch-inactive':''} ${muted?'level-muted':''}" data-id="${n.id}" data-type="${n.type}" data-branch="${branch?.id||''}" style="left:${p.x}px;top:${p.y}px;width:${size.w}px;height:${size.h}px;min-height:0;--node-header-height:${size.ownH||54}px;--type-color:${t[3]};--branch-color:${branch?.color||t[3]}" tabindex="${muted?'-1':'0'}" aria-disabled="${muted}" aria-label="${esc(n.label)}">${heading}${sourceMark}</article>`}).join('');
    renderCollectionGroupControls();
    $$('.node').forEach(n=>{n.onpointerdown=e=>startNodeInteraction(e,n.dataset.id);n.onclick=e=>{e.stopPropagation();if(suppressNodeClickId===n.dataset.id){suppressNodeClickId=null;return}if(!pending)select({kind:'node',id:n.dataset.id})}});
    $$('.collection-toggle').forEach(toggle=>{toggle.onpointerdown=e=>{e.preventDefault();e.stopPropagation()};toggle.onclick=e=>{e.preventDefault();e.stopPropagation();openCollectionId=openCollectionId===toggle.dataset.id?null:toggle.dataset.id;renderNodes();renderCollectionMenu()}});renderCollectionMenu();
    $$('.node-source-mark').forEach(mark=>{mark.onpointerdown=e=>{e.preventDefault();e.stopPropagation()};mark.onclick=e=>{e.preventDefault();e.stopPropagation();showSourceInfo(mark.dataset.id)}});
  }
  function updateNodePositions(){$$('.node').forEach(el=>{const n=model.elements.find(x=>x.id===el.dataset.id);if(!n)return;const p=nodePosition(n);el.style.left=`${p.x}px`;el.style.top=`${p.y}px`})}
  function renderGroups(){els.groups.innerHTML='';const levels=model.groups.filter(g=>g.kind==='level'||!g.kind);els.filters.innerHTML='<span class="dimension-caption">Staatliche Ebenen</span>'+levels.map(g=>`<div class="level-filter ${selected?.kind==='group'&&selected.id===g.id?'selected':''}" data-id="${g.id}"><input type="checkbox" ${g.active===false?'':'checked'} aria-label="${esc(g.label)} anzeigen"><button class="level-name" type="button" draggable="true" aria-label="${esc(g.label)} bearbeiten oder zum Sortieren ziehen">${esc(g.label)}</button></div>`).join('');els.filters.querySelectorAll('.level-filter').forEach(el=>{const g=model.groups.find(g=>g.id===el.dataset.id),name=el.querySelector('.level-name');el.querySelector('input').onchange=e=>{g.active=e.target.checked;if(!g.active&&selected?.kind==='node'){const n=model.elements.find(n=>n.id===selected.id);if(n&&!nodeLevelActive(n))selected=null}if(!g.active&&selected?.kind==='edge'){const r=model.relations.find(r=>r.id===selected.id),s=model.elements.find(n=>n.id===r?.source),t=model.elements.find(n=>n.id===r?.target);if(!nodeLevelActive(s)||!nodeLevelActive(t))selected=null}renderNodes();renderEdges();renderInspector();save()};name.onclick=()=>select({kind:'group',id:g.id});name.ondragstart=e=>{e.dataTransfer.effectAllowed='move';e.dataTransfer.setData('text/level-id',g.id);el.classList.add('is-dragging')};el.ondragover=e=>{e.preventDefault();e.dataTransfer.dropEffect='move';els.filters.querySelectorAll('.level-filter').forEach(item=>item.classList.remove('drop-before','drop-after'));el.classList.add(e.clientY<el.getBoundingClientRect().top+el.offsetHeight/2?'drop-before':'drop-after')};el.ondrop=e=>{e.preventDefault();const sourceId=e.dataTransfer.getData('text/level-id');if(!sourceId||sourceId===g.id)return;const from=model.groups.findIndex(item=>item.id===sourceId),moved=model.groups.splice(from,1)[0],target=model.groups.findIndex(item=>item.id===g.id),after=e.clientY>=el.getBoundingClientRect().top+el.offsetHeight/2;model.groups.splice(target+(after?1:0),0,moved);renderGroups();save()};name.ondragend=()=>{els.filters.querySelectorAll('.level-filter').forEach(item=>item.classList.remove('is-dragging','drop-before','drop-after'))}});els.branches.innerHTML='<span class="dimension-caption">Gewalten</span>'+ensureBranches().map(branch=>`<label class="branch-filter ${branch.active?'':'inactive'} ${selected?.kind==='branch'&&selected.id===branch.id?'selected':''}" data-id="${branch.id}" style="--branch-color:${branch.color}"><input type="checkbox" ${branch.active?'checked':''} aria-label="Farbdarstellung ${branch.label}"><span>${branch.label}</span></label>`).join('');els.branches.querySelectorAll('.branch-filter').forEach(control=>{const branch=ensureBranches().find(branch=>branch.id===control.dataset.id);control.onclick=e=>{if(e.target.matches('input'))return;select({kind:'branch',id:branch.id})};control.querySelector('input').onchange=e=>{branch.active=e.target.checked;selected={kind:'branch',id:branch.id};els.branches.querySelectorAll('.branch-filter').forEach(item=>item.classList.toggle('selected',item===control));control.classList.toggle('inactive',!branch.active);renderNodes();renderInspector();save()}})}
  function updateGroupPositions(){$$('.group').forEach(el=>{const g=model.groups.find(x=>x.id===el.dataset.id);if(!g)return;el.style.left=`${g.x}px`;el.style.top=`${g.y}px`;el.style.width=`${g.w}px`;el.style.height=`${g.h}px`})}
  function syncCanvasLayers(){updateGroupPositions();updateNodePositions();renderEdges()}
  function segmentHits(a,b,rect){if(a.x===b.x)return a.x>=rect.l&&a.x<=rect.r&&Math.max(a.y,b.y)>=rect.t&&Math.min(a.y,b.y)<=rect.b;return a.y>=rect.t&&a.y<=rect.b&&Math.max(a.x,b.x)>=rect.l&&Math.min(a.x,b.x)<=rect.r}
  function routeScore(points,obstacles){let hits=0;for(let i=1;i<points.length;i++)obstacles.forEach(r=>{if(segmentHits(points[i-1],points[i],r))hits++});const length=points.slice(1).reduce((sum,p,i)=>sum+Math.abs(p.x-points[i].x)+Math.abs(p.y-points[i].y),0);return hits*100000+length}
  function relationObstacles(s,t){return model.elements.filter(n=>nodeVisible(n)&&n!==s&&n!==t&&!isDescendant(s.id,n.id)&&!isDescendant(t.id,n.id)).map(nodeBox)}
  function relationBlocked(r,points){const s=model.elements.find(n=>n.id===r.source),t=model.elements.find(n=>n.id===r.target);if(!s||!t)return true;return DiagramGeometry.pathBlocked(points,relationObstacles(s,t),connectionBox(s),connectionBox(t))}
  function routeRelation(r){const s=model.elements.find(n=>n.id===r.source),t=model.elements.find(n=>n.id===r.target);if(!s||!t)return[];const sourceBox=connectionBox(s),targetBox=connectionBox(t),ports=relationPorts(r),clean=points=>preservedPointRelationId===r.id?points:DiagramGeometry.simplify(points),finish=(points,exact=false)=>clean(DiagramGeometry.ensureTargetLead(DiagramGeometry.ensureSourceLead(points,sourceBox,ports.source,undefined,exact),targetBox,ports.target,undefined,exact)),obstacles=relationObstacles(s,t);const safe=points=>!relationBlocked(r,points),previewing=drag?.id===r.id&&(drag.kind==='corner'||drag.kind==='segment');if(r.visual?.manualRoute){if(r.visual.freeRoute){const p=r.visual.manualRoute.map(v=>({...v}));p[0]=DiagramGeometry.anchor(sourceBox,ports.source);p[p.length-1]=DiagramGeometry.anchor(targetBox,ports.target);const candidate=finish(p);if(safe(candidate)||previewing)return candidate}const manual=DiagramGeometry.attachRoute(r.visual.manualRoute,DiagramGeometry.anchor(sourceBox,ports.source),DiagramGeometry.anchor(targetBox,ports.target));if(manual){const candidate=finish(manual);if(safe(candidate)||previewing)return candidate}}const result=DiagramGeometry.route(sourceBox,targetBox,ports.source,ports.target,obstacles),automatic=finish(result.points,true);if(!result.blocked&&safe(automatic))return automatic;const a=DiagramGeometry.anchor(sourceBox,ports.source),b=DiagramGeometry.anchor(targetBox,ports.target),fallback=finish(DiagramGeometry.orthogonalize(Math.abs(a.x-b.x)>=Math.abs(a.y-b.y)?[a,{x:b.x,y:a.y},b]:[a,{x:a.x,y:b.y},b]));return fallback.length>1?fallback:[]}
  function pointOnRoute(points,t){const lengths=points.slice(1).map((p,i)=>Math.hypot(p.x-points[i].x,p.y-points[i].y)),total=lengths.reduce((a,b)=>a+b,0);let target=total*t;for(let i=0;i<lengths.length;i++){if(target<=lengths[i]){const a=points[i],b=points[i+1],f=lengths[i]?target/lengths[i]:0;return{x:a.x+(b.x-a.x)*f,y:a.y+(b.y-a.y)*f}}target-=lengths[i]}return points.at(-1)}
  function labelCursor(points,t){const lengths=points.slice(1).map((p,i)=>Math.hypot(p.x-points[i].x,p.y-points[i].y)),total=lengths.reduce((a,b)=>a+b,0);let target=total*t;for(let i=0;i<lengths.length;i++){if(target<=lengths[i]){const a=points[i],b=points[i+1];return Math.abs(b.y-a.y)>Math.abs(b.x-a.x)?'ns-resize':'ew-resize'}target-=lengths[i]}return'ew-resize'}
  let lastLinePress=null,preservedPointRelationId=null,preservedPoint=null;
  function handleRoutePress(e,id){const now=performance.now(),double=lastLinePress&&lastLinePress.id===id&&now-lastLinePress.time<420&&Math.hypot(e.clientX-lastLinePress.x,e.clientY-lastLinePress.y)<9;lastLinePress=double?null:{id,time:now,x:e.clientX,y:e.clientY};if(double){addRoutePoint(e,id);return true}return false}
  function addRoutePoint(e,id){e.preventDefault();e.stopPropagation();const r=model.relations.find(r=>r.id===id);if(!r)return;const inserted=DiagramGeometry.insertPoint(routeRelation(r),point(e));if(inserted.index<0)return;selected={kind:'edge',id};preservedPointRelationId=id;preservedPoint={id,x:inserted.point.x,y:inserted.point.y};r.visual=r.visual||{};r.visual.manualRoute=inserted.points;r.visual.freeRoute=true;render();save()}
  function boxesOverlap(a,b){return a.l<b.r&&a.r>b.l&&a.t<b.b&&a.b>b.t}
  function sourceMarkerPosition(r,label,relations,routeCache,nodeBoxes){const proposed={l:label.x-8,r:label.x+8,t:label.y-36,b:label.y-16},nodeCollision=nodeBoxes.some(n=>boxesOverlap(proposed,n)),lineCollision=relations.some(other=>{if(other.id===r.id)return false;const points=routeCache.get(other.id)||[];return points.slice(1).some((p,i)=>DiagramGeometry.intersectsBox(points[i],p,proposed))});if(nodeCollision||lineCollision)return{x:label.x-Math.max(16,r.type.length*3.4+10),y:label.y-7,inline:true};return{x:label.x,y:label.y-23,inline:false}}
  function renderEdges(){
    const relations=visibleRelations(),routeCache=new Map(relations.map(r=>[r.id,routeRelation(r)])),nodeBoxes=model.elements.filter(nodeVisible).map(nodeBox),curves=relations.map(r=>{
      const points=routeCache.get(r.id);if(!points.length)return'';
      const hitPath=points.map((p,i)=>`${i?'L':'M'} ${p.x} ${p.y}`).join(' '),visiblePoints=DiagramGeometry.trimTarget(points),path=visiblePoints.map((p,i)=>`${i?'L':'M'} ${p.x} ${p.y}`).join(' '),label=pointOnRoute(points,r.visual?.labelPosition??.5),sourced=hasSource(r),labelWidth=Math.max(42,r.type.length*6.8+18),sel=selected?.kind==='edge'&&selected.id===r.id,muted=!nodeLevelActive(model.elements.find(n=>n.id===r.source))||!nodeLevelActive(model.elements.find(n=>n.id===r.target)),invalid=drag?.id===r.id&&drag.invalid;
      return `<path class="edge-hit ${muted?'level-muted':''}" data-id="${r.id}" d="${hitPath}"/><path class="edge-path ${muted?'level-muted':''} ${sel?'edge-selected':''} ${invalid?'edge-invalid':''}" data-id="${r.id}" d="${path}" style="--edge-color:${relationColor(r)}"/><g class="edge-label-control ${sourced?'has-source':'without-source'} ${muted?'level-muted':''}" data-id="${r.id}"><rect class="edge-label-bg" data-id="${r.id}" x="${label.x-labelWidth/2}" y="${label.y-22}" width="${labelWidth}" height="22" rx="5"/><text class="edge-label ${muted?'level-muted':''}" data-id="${r.id}" x="${label.x}" y="${label.y-7}" text-anchor="middle" style="cursor:${labelCursor(points,r.visual?.labelPosition??.5)}">${esc(r.type)}</text></g>`
    }).join('');
    const draft=pending?`<path class="drag-line" d="M ${pending.startX} ${pending.startY} L ${pending.x} ${pending.y}"/>`:'';els.edges.innerHTML=curves+draft;renderEndpointHandles();$$('.edge-hit').forEach(p=>{p.onpointerdown=e=>{if(e.button===0)handleRoutePress(e,p.dataset.id)};p.onclick=e=>{e.stopPropagation();select({kind:'edge',id:p.dataset.id})};p.ondblclick=e=>addRoutePoint(e,p.dataset.id)});$$('.edge-label').forEach(label=>{label.onpointerdown=e=>{e.preventDefault();e.stopPropagation();label.dataset.pressX=e.clientX;label.dataset.pressY=e.clientY;const r=model.relations.find(x=>x.id===label.dataset.id);selected={kind:'edge',id:r.id};drag={kind:'edge-label',id:r.id,startX:e.clientX,start:r.visual?.labelPosition??.5};els.canvas.setPointerCapture(e.pointerId);renderEndpointHandles();renderInspector()};label.onclick=e=>{e.stopPropagation();if(!hasSource(model.relations.find(r=>r.id===label.dataset.id))||Math.hypot(e.clientX-Number(label.dataset.pressX),e.clientY-Number(label.dataset.pressY))>4)return;showSourceInfo(label.dataset.id)}});$$('.edge-label-bg').forEach(bg=>{bg.onpointerdown=e=>{e.preventDefault();e.stopPropagation()};bg.onclick=e=>{e.preventDefault();e.stopPropagation();showSourceInfo(bg.dataset.id)}})
  }
  function normalizeRelation(r){if(r?.visual?.manualRoute)r.visual.manualRoute=DiagramGeometry.simplify(r.visual.manualRoute)}
  function select(s){if(preservedPointRelationId&&s?.id!==preservedPointRelationId){normalizeRelation(model.relations.find(r=>r.id===preservedPointRelationId));preservedPointRelationId=null;preservedPoint=null;save()}selected=s;render()}
  function isDescendant(candidateId,parentId){let current=model.elements.find(n=>n.id===candidateId);while(current?.parentId){if(current.parentId===parentId)return true;current=model.elements.find(n=>n.id===current.parentId)}return false}
  function renderInspector(){syncPrecisionInspector();const forms=[els.paletteInfo,els.nodeForm,els.edgeForm,els.groupForm];forms.forEach(f=>f.hidden=true);$$('.palette-item').forEach(b=>b.classList.toggle('is-selected',selected?.kind==='palette'&&selected.id===b.dataset.type));$('#inspectorEmpty').hidden=!!selected;if(!selected)return;if(selected.kind==='palette'){const type=TYPES.find(t=>t[0]===selected.id),info=TYPE_INFO[selected.id];if(!type||!info){selected=null;return renderInspector()}els.paletteInfo.hidden=false;$('#inspectorTitle').textContent='Elementtyp';$('#paletteInfoTitle').textContent=type[1];$('#paletteInfoDescription').textContent=info[0];$('#paletteInfoGuidance').textContent=info[1];$('#paletteInfoExamples').textContent=info[2];return}const obj=getSelected();if(!obj){selected=null;return renderInspector()}const form=selected.kind==='node'?els.nodeForm:selected.kind==='edge'?els.edgeForm:els.groupForm;form.hidden=false;$('#inspectorTitle').textContent=selected.kind==='node'?'Element':selected.kind==='edge'?'Beziehung':'Administrative Ebene';if(selected.kind==='node'){form.elements.label.value=obj.label;form.elements.type.value=obj.type;const items=collectionItems(obj);$('#collectionItemsField').hidden=obj.type!=='collection';$('#editCollectionItems').textContent=`Inhalte bearbeiten${items.length?` (${items.length})`:''}`;$('#collectionItemsSummary').textContent=items.length?`${items.length} Institution${items.length===1?'':'en'} in dieser Sammlung`:'Noch keine Institution enthalten';const parents=model.elements.filter(n=>n.id!==obj.id&&!isDescendant(n.id,obj.id));form.elements.parentId.innerHTML='<option value="">Keiner Elementgruppe</option>'+parents.map(n=>`<option value="${n.id}">${esc(n.label)}</option>`).join('');form.elements.parentId.value=obj.parentId||'';form.elements.groupId.innerHTML='<option value="">Keiner administrativen Ebene</option>'+model.groups.filter(g=>g.kind==='level'||!g.kind).map(g=>`<option value="${g.id}">${esc(g.label)}</option>`).join('');form.elements.groupId.value=obj.groupId||'';['level','branch','term','members','structure'].forEach(k=>form.elements[k].value=obj.properties[k]||'');form.elements.independent.checked=!!obj.properties.independent}else if(selected.kind==='edge'){form.elements.relation.value=obj.type;const source=sourceData(obj),summary=[source.law,source.citation].filter(Boolean).join(' · ')||source.note,$summary=$('#relationSourceSummary');$('#editRelationSource').textContent=hasSource(obj)?'Quelle bearbeiten':'Quelle hinzufügen';$summary.textContent=summary;$summary.hidden=!summary;const s=model.elements.find(n=>n.id===obj.source),t=model.elements.find(n=>n.id===obj.target);$('#edgeStatement').textContent=`${s?.label} → ${obj.type} → ${t?.label}`}else form.elements.label.value=obj.label}
  function syncCustomRelationInspector(){if(selected?.kind!=='edge')return;const r=getSelected(),known=selectableRelationTypes().includes(r.type),form=els.edgeForm;form.elements.relation.value=known?r.type:'__custom__';$('#customRelationInspectorField').hidden=known;form.elements.customRelation.value=known?'':r.type}
  function syncNodeSourceInspector(){if(selected?.kind!=='node')return;const node=getSelected(),source=sourceData(node),summary=[source.law,source.citation].filter(Boolean).join(' · ')||source.note,$summary=$('#nodeSourceSummary');$('#editNodeSource').textContent=hasSource(node)?'Quellen zur Funktion bearbeiten':'Quelle zur Funktion hinzufügen';$summary.textContent=summary;$summary.hidden=!summary}
  const renderInspectorBase=renderInspector;
  renderInspector=function(){if(selected?.kind==='branch'){[els.paletteInfo,els.nodeForm,els.edgeForm,els.groupForm,els.branchForm].forEach(form=>form.hidden=true);const branch=getSelected();if(!branch){selected=null;return renderInspector()}$('#inspectorEmpty').hidden=true;$('#inspectorTitle').textContent='Gewalt';els.branchForm.hidden=false;els.branchForm.elements.label.value=branch.label;els.branchForm.elements.active.checked=branch.active;els.branchForm.elements.color.value=branch.color;$('#branchColorValue').value=branch.color.toUpperCase();return}renderInspectorBase();els.branchForm.hidden=true;syncCustomRelationInspector();syncNodeSourceInspector();const node=selected?.kind==='node'?getSelected():null,field=$('#collectionGroupField'),parentField=els.nodeForm.elements.parentId?.closest('label'),typeField=els.nodeForm.elements.type?.closest('label'),automaticParent=node?.type==='collection'&&model.elements.find(item=>item.id===node.parentId)?.type==='collection-group';field.hidden=node?.type!=='collection-group';if(parentField)parentField.hidden=!!automaticParent;if(typeField)typeField.hidden=!!automaticParent;if(node?.type==='collection-group'){const count=allChildrenOf(node.id).filter(child=>child.type==='collection').length;$('#editCollectionGroup').textContent=`Inhalte bearbeiten${count?` (${count})`:''}`;$('#collectionGroupSummary').textContent=`${count} Sammlung${count===1?'':'en'} · eine davon wird angezeigt`}};
  const renderInspectorWithoutCollectionParents=renderInspector;
  renderInspector=function(){renderInspectorWithoutCollectionParents();if(selected?.kind!=='node')return;const node=getSelected(),select=els.nodeForm.elements.parentId;for(const option of [...select.options]){if(!option.value)continue;const candidate=model.elements.find(item=>item.id===option.value);if(candidate?.type==='collection'||candidate?.type==='collection-group'&&['collection','collection-group'].includes(node?.type))option.remove()}};
  function updateFromInspector(e){if(!selected)return;const obj=getSelected(),name=e.target.name;if(selected.kind==='node'){if(name==='parentId'){const previous=nodePosition(obj);obj.parentId=e.target.value;if(obj.parentId)obj.groupId='';else{obj.visual.x=previous.x;obj.visual.y=previous.y}centerElementsView()}else if(name==='label'||name==='type'||name==='groupId')obj[name]=e.target.value;else if(name==='items')obj.properties.items=e.target.value.split(/\r?\n/).map(item=>item.trim()).filter(Boolean).slice(0,40);else obj.properties[name]=e.target.type==='checkbox'?e.target.checked:e.target.value}else if(selected.kind==='edge'){if(name==='relation'){obj.type=e.target.value==='__custom__'?'':e.target.value;obj.category=obj.type?categoryForType(obj.type,obj.category||activeRelationCategory()):obj.category||activeRelationCategory()}else if(name==='customRelation')obj.type=e.target.value}else if(selected.kind==='branch'){if(name==='active')obj.active=e.target.checked;else if(name==='color')obj.color=e.target.value}else if(name==='kind'||name==='parentGroupId')obj[name]=e.target.value;else obj.label=e.target.value;renderNodes();renderGroups();renderEdges();renderInspector();save()}
  function getSelected(){return selected?.kind==='node'?model.elements.find(x=>x.id===selected.id):selected?.kind==='edge'?model.relations.find(x=>x.id===selected.id):selected?.kind==='branch'?ensureBranches().find(x=>x.id===selected.id):model.groups.find(x=>x.id===selected.id)}
  function removeSelected(){if(!selected||selected.kind==='palette')return;if(selected.kind==='node'){const node=getSelected();if(node.type==='collection-group'){const ordinary=allChildrenOf(node.id).filter(child=>child.type!=='collection').map(child=>({child,position:nodePosition(child)})),removed=new Set([node.id]);const collect=id=>allChildrenOf(id).filter(child=>child.type==='collection').forEach(child=>{removed.add(child.id);collect(child.id)});collect(node.id);ordinary.forEach(({child,position})=>{child.visual.x=position.x;child.visual.y=position.y;child.parentId=''});model.elements=model.elements.filter(item=>!removed.has(item.id));model.relations=model.relations.filter(r=>!removed.has(r.source)&&!removed.has(r.target))}else{model.elements.filter(x=>x.parentId===selected.id).forEach(x=>{const p=nodePosition(x);x.visual.x=p.x;x.visual.y=p.y;x.parentId=''});model.elements=model.elements.filter(x=>x.id!==selected.id);model.relations=model.relations.filter(x=>x.source!==selected.id&&x.target!==selected.id)}}else if(selected.kind==='edge')model.relations=model.relations.filter(x=>x.id!==selected.id);else{model.elements.forEach(x=>{if(x.groupId===selected.id)x.groupId=''});model.groups.forEach(x=>{if(x.parentGroupId===selected.id)x.parentGroupId=''});model.groups=model.groups.filter(x=>x.id!==selected.id)}selected=null;render();save()}
  const removeSelectedBase=removeSelected;
  removeSelected=function(){removeSelectedBase()};
  function setMode(mode,persist=true){model.mode=mode;const advanced=mode==='advanced';if(!advanced&&(selected?.kind==='group'||selected?.kind==='branch'||selected?.kind==='node'&&!nodeVisible(getSelected())||selected?.kind==='edge'&&!visibleRelations().some(r=>r.id===selected.id)))selected=null;$$('.mode-switch button').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));$$('.advanced-fields').forEach(x=>x.hidden=!advanced);$$('.advanced-only').forEach(x=>x.hidden=!advanced);fillRelationTypes();if(persist){pending=null;renderGroups();renderNodes();renderEdges();renderInspector();save()}}
  function buildVisibleExportSvg(){const allRelations=model.relations,allElements=model.elements,relations=visibleRelations(),elements=allElements.filter(nodeVisible).map(n=>n.type==='collection'?{...n,label:[n.label,...collectionItems(n)].join('\n'),properties:{...n.properties,items:[]}}:n);model.relations=relations;model.elements=elements;try{return buildExportSvg().replace(/<text[^>]*>§<\/text>/g,'')}finally{model.relations=allRelations;model.elements=allElements}}
  function exportWarnings(){const warnings=[];if(!model.elements.length)warnings.push('Das Modell enthält noch keine Elemente.');const unnamed=model.elements.filter(n=>!n.label.trim()).length;if(unnamed)warnings.push(unnamed+' Elemente sind nicht benannt.');const boxes=model.elements.map(nodeBox),outside=boxes.filter(b=>b.l<0||b.t<0||b.r>4000||b.b>3000).length;if(outside)warnings.push(outside+' Elemente liegen teilweise außerhalb der Modellfläche; der Export schließt sie ein.');let conflicts=0,unroutable=0;for(const r of model.relations){const points=routeRelation(r);if(points.length<2){unroutable++;continue}const obstacles=model.elements.filter(n=>n.id!==r.source&&n.id!==r.target&&!isDescendant(r.source,n.id)&&!isDescendant(r.target,n.id)).map(nodeBox);if(points.slice(1).some((p,i)=>DiagramGeometry.blocked(points[i],p,obstacles)))conflicts++}if(unroutable)warnings.push(unroutable+' Verbindungen können in der aktuellen Anordnung nicht außerhalb der Elemente geführt werden.');if(conflicts)warnings.push(conflicts+' Verbindungen durchqueren noch ein Element. Verschiebe das Element oder ändere den Pfeilanschluss.');let overlaps=0;for(let i=0;i<model.elements.length;i++)for(let j=i+1;j<model.elements.length;j++){const a=model.elements[i],b=model.elements[j],x=boxes[i],y=boxes[j];if(!isDescendant(a.id,b.id)&&!isDescendant(b.id,a.id)&&x.l<y.r&&x.r>y.l&&x.t<y.b&&x.b>y.t)overlaps++}if(overlaps)warnings.push(overlaps+' Elementpaare überlappen sich.');if($('#exportPaper').value!=='auto'){const b=exportBounds(),paper=$('#exportPaper').value==='a3'?[273,396]:[186,273];if($('#exportOrientation').value==='landscape')paper.reverse();const pt=13*Math.min(paper[0]/(b.r-b.l),paper[1]/(b.b-b.t+54))*72/25.4;if(pt<8)warnings.push('Die kleinste Schrift wird beim Einpassen etwa '+pt.toFixed(1)+' pt groß. Größeres Papier oder SVG verwenden.')}return warnings}
  function openExport(){const warnings=exportWarnings(),box=$('#exportValidation');box.classList.toggle('has-warnings',warnings.length>0);box.textContent=warnings.length?warnings.join(' '):'Das Modell ist für den Export bereit.';$('#exportDialog').showModal()}
  function exportBounds(){const boxes=model.elements.filter(nodeVisible).map(nodeBox);visibleRelations().forEach(r=>{const pts=routeRelation(r);pts.forEach(p=>boxes.push({l:p.x-12,t:p.y-12,r:p.x+12,b:p.y+12}));if(pts.length){const p=pointOnRoute(pts,r.visual?.labelPosition??.5),half=r.type.length*3.8;boxes.push({l:p.x-half,t:p.y-24,r:p.x+half,b:p.y+5})}});if(!boxes.length)return{l:0,t:0,r:800,b:600};return{l:Math.min(...boxes.map(b=>b.l))-36,t:Math.min(...boxes.map(b=>b.t))-36,r:Math.max(...boxes.map(b=>b.r))+36,b:Math.max(...boxes.map(b=>b.b))+36}}
  function buildExportSvg(){const b=exportBounds(),title=$('#exportTitle').checked,top=b.t-(title?54:0),w=b.r-b.l,h=b.b-top,paper=$('#exportPaper').value,landscape=$('#exportOrientation').value==='landscape',mm=paper==='a3'?[297,420]:[210,297],size=landscape?mm.reverse():mm,color=$('#exportTheme').value==='color',attrs=paper==='auto'?'width="'+Math.ceil(w)+'" height="'+Math.ceil(h)+'"':'width="'+size[0]+'mm" height="'+size[1]+'mm"';const groups='';const nodes=[...model.elements].sort((a,b)=>nodeDepth(a)-nodeDepth(b)).map(n=>{const p=nodePosition(n),z=nodeSize(n),t=TYPES.find(x=>x[0]===n.type),lines=wrapLabel(n.label,z.w-24);return '<rect x="'+(p.x-z.w/2)+'" y="'+(p.y-z.h/2)+'" width="'+z.w+'" height="'+z.h+'" rx="3" fill="white" stroke="'+(color?t[3]:'#213633')+'"/><text font-size="16" font-weight="700" text-anchor="middle" stroke="none">'+lines.map((line,i)=>'<tspan x="'+p.x+'" y="'+(p.y-z.h/2+z.ownH/2-(lines.length-1)*19/2+5.5+i*19)+'">'+esc(line)+'</tspan>').join('')+'</text>'}).join('');const edges=model.relations.map(r=>{const pts=routeRelation(r);if(!pts.length)return'';const d=pts.map((p,i)=>(i?'L':'M')+' '+p.x+' '+p.y).join(' '),p=pointOnRoute(pts,r.visual?.labelPosition??.5);return '<path d="'+d+'" fill="none" marker-end="url(#arrowExport)"/><text x="'+p.x+'" y="'+(p.y-7)+'" text-anchor="middle" font-size="13" font-weight="600" paint-order="stroke" stroke="white" stroke-width="5">'+esc(r.type)+'</text>'+(r.properties?.source?.trim()?'<text x="'+p.x+'" y="'+(p.y-23)+'" text-anchor="middle" font-size="12" font-weight="700" stroke="none">§</text>':'')}).join('');return '<svg xmlns="http://www.w3.org/2000/svg" '+attrs+' viewBox="'+b.l+' '+top+' '+w+' '+h+'" font-family="Arial, Helvetica, sans-serif" fill="#213633" stroke="#213633" stroke-width="1.5"><defs><marker id="arrowExport" markerWidth="12" markerHeight="10" refX="11" refY="4" orient="auto" markerUnits="userSpaceOnUse"><path d="M0 0 L11 4 L0 8" fill="none"/></marker></defs><rect x="'+b.l+'" y="'+top+'" width="'+w+'" height="'+h+'" fill="white" stroke="none"/>'+groups+nodes+edges+(title?'<text x="'+((b.l+b.r)/2)+'" y="'+(top+32)+'" font-size="'+Math.min(28,(w-24)/(Math.max(1,model.title.length)*.62))+'" font-weight="700" text-anchor="middle" stroke="none">'+esc(model.title)+'</text>':'')+'</svg>'}
  function exportSvg(){const svg=buildVisibleExportSvg(),blob=new Blob([svg],{type:'image/svg+xml'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=(model.title||'staatsmodell').toLowerCase().replace(/[^a-z0-9äöüß]+/gi,'-')+'.svg';a.click();URL.revokeObjectURL(a.href)}
  function printPdf(){const svg=buildVisibleExportSvg(),paper=$('#exportPaper').value==='a3'?'A3':'A4',orientation=$('#exportOrientation').value,win=window.open('','_blank');if(!win){alert('Das Druckfenster wurde vom Browser blockiert.');return}win.document.write(`<!doctype html><html><head><title>${esc(model.title)}</title><style>@page{size:${paper} ${orientation};margin:12mm}html,body{margin:0;height:100%}body{display:grid;place-items:center}svg{max-width:100%;max-height:100%;width:100%;height:100%}</style></head><body>${svg}<script>onload=()=>setTimeout(()=>print(),150)<\/script></body></html>`);win.document.close()}
  function exportModel(){const blob=new Blob([JSON.stringify(model,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=(model.title||'staatsmodell').toLowerCase().replace(/[^a-z0-9äöüß]+/gi,'-')+'.staatsmodell.json';a.click();URL.revokeObjectURL(a.href)}
  function adoptImportedModel(data){model=validateModel(data);selected=null;pending=null;$('#modelTitle').value=model.title;setMode(model.mode,false);setZoom(model.view?.zoom||1,false);render();save()}
  function parseImportedJson(text){const cleaned=String(text).trim().replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim();if(!cleaned)throw Error('Bitte füge zuerst JSON-Code ein.');return JSON.parse(cleaned)}
  function importModel(e){const file=e.target.files[0];if(!file)return;file.text().then(text=>adoptImportedModel(parseImportedJson(text))).catch(error=>alert('Import nicht möglich: '+error.message));e.target.value=''}
  function importModelCode(){const errorBox=$('#jsonImportError');try{adoptImportedModel(parseImportedJson($('#jsonImportCode').value));errorBox.hidden=true;$('#jsonImportDialog').close()}catch(error){const syntax=error instanceof SyntaxError?'Der eingefügte Text ist kein gültiges JSON. ':'';errorBox.textContent=syntax+error.message;errorBox.hidden=false;$('#jsonImportCode').focus()}}
  function generationPrompt(state){const elementTypes=TYPES.map(([id,label])=>`${id} (${label})`).join(', '),relations=Object.entries(RELATIONS).map(([category,types])=>`- ${category}: ${types.join(', ')}`).join('\n');return `Erstelle ein fachlich korrektes Staatsaufbau-Diagramm für „${state}“ als JSON-Datei für den Schola-Staatsbaukasten.

Recherchiere den gegenwärtigen verfassungsrechtlichen und institutionellen Stand sorgfältig anhand vorrangig amtlicher Quellen. Erfinde keine Zuständigkeiten, Beziehungen, Fundstellen oder URLs. Wenn eine Angabe nicht zuverlässig feststellbar ist, lasse das optionale Feld weg. Das Ergebnis soll für Schülerinnen und Schüler verständlich, zugleich fachlich präzise und hinreichend vollständig sein.

AUSGABEFORMAT
Antworte ausschließlich mit einem einzigen gültigen JSON-Objekt: kein Markdown, keine Code-Fences, keine Einleitung und keine Erläuterung nach dem JSON. Verwende UTF-8, doppelte Anführungszeichen, keine Kommentare und keine nachgestellten Kommas. Das Objekt muss direkt importierbar sein und exakt diese Wurzelstruktur besitzen:
{
  "version": 1,
  "title": "Staatsaufbau von ${state}",
  "mode": "advanced",
  "view": { "zoom": 1, "relationCategory": "Besetzung und Legitimation" },
  "groups": [],
  "branches": [
    { "id": "legislative", "label": "Legislative", "color": "#b38a35", "active": true },
    { "id": "executive", "label": "Exekutive", "color": "#c76842", "active": true },
    { "id": "judiciary", "label": "Judikative", "color": "#557060", "active": true }
  ],
  "elements": [],
  "relations": []
}

ALLGEMEINE REGELN
- Halte das Diagramm didaktisch übersichtlich: im Regelfall höchstens 45 Elemente und 90 Beziehungen. Fasse nachgeordnete Institutionen zusammen, wenn zusätzliche Einzelboxen keinen wesentlichen Erkenntnisgewinn bringen. Die technischen Obergrenzen von 200 Elementen und 500 Beziehungen dürfen nur in begründeten Ausnahmefällen ausgeschöpft werden.
- Jede id muss im gesamten Dokument eindeutig sein und nur Buchstaben, Ziffern, Unterstriche oder Bindestriche enthalten. Verwende kurze sprechende IDs ohne Leerzeichen, etwa "volk", "parlament" oder "verfassungsgericht".
- Alle Beziehungen verweisen mit source und target auf vorhandene Element-IDs. Keine Selbstbeziehungen.
- Übernimm die drei Einträge in branches vollständig. Die properties.branch eines Elements darf "Legislative", "Exekutive" oder "Judikative" lauten. Die Farben dienen nur der Darstellung und ersetzen keine inhaltliche Zuordnung.
- Stelle nur reale Institutionen und rechtlich oder politisch bedeutsame Beziehungen dar. Vermeide Dubletten.
- Kodiere dieselbe Aussage niemals doppelt. Wenn ein Element über parentId in einem übergeordneten Element enthalten ist, erstelle nicht zusätzlich eine Beziehung wie "umfasst", "besteht aus" oder "gehört an" zwischen genau diesen beiden Elementen. parentId ist bereits die sichtbare Gruppierungsbeziehung.
- Vermeide sternförmige Massenverbindungen von einem Sammelorgan zu sämtlichen Unterbehörden. Zeige normalerweise höchstens 4 bis 8 repräsentative Unterorgane je Sammelorgan; fasse den Rest in einer gemeinsamen, präzise bezeichneten Institution zusammen.
- Bezeichnungen, Notizen und Beziehungsformulierungen stehen auf Deutsch; amtliche Eigennamen dürfen in der Originalsprache ergänzt werden.

ELEMENTE
Jedes Element hat diese Grundform:
{
  "id": "eindeutige-id",
  "type": "institution",
  "label": "Amtliche oder geläufige Bezeichnung",
  "groupId": "optionale-administrative-ebene",
  "parentId": "optionales-übergeordnetes-element",
  "properties": {
    "level": "optionale institutionelle Ebene",
    "branch": "optional: Legislative, Exekutive, Judikative oder sonstige",
    "term": "optionale Amtszeit",
    "members": "optionale Zusammensetzung",
    "structure": "optionale kurze Strukturangabe",
    "independent": false,
    "items": ["nur bei type collection: eine Institution", "weitere Institution"],
    "activeCollectionId": "nur bei type collection-group: ID der anfangs sichtbaren untergeordneten Sammlung",
    "sources": [
      { "law": "Name des Gesetzes oder der Verfassung", "citation": "Art./§ und Absatz", "url": "https://amtliche-quelle.example/...", "note": "Kurze Erklärung der daraus folgenden Funktion" }
    ]
  },
  "visual": { "x": 400, "y": 200, "width": 180 }
}
Zulässige type-Werte sind ausschließlich: ${elementTypes}.
Nutze parentId nur für echte inhaltliche Untergliederungen, etwa Kammern innerhalb eines Parlaments oder ausgewählte Teile eines Kabinetts. Unterelemente erhalten weiterhin eigene eindeutige Positionen. Ein parentId-Unterelement benötigt keine zusätzliche Beziehung zum Elternobjekt. Quellen in properties.sources sind optional und dürfen mehrfach vorkommen. URLs müssen vollständige sichere https-Adressen sein.
Nutze type "collection" für eine aufklappbare Institutionssammlung, wenn mehrere Institutionen namentlich abrufbar sein sollen, aber keine individuellen Beziehungen, Quellen oder Eigenschaften benötigen. Schreibe ihre Namen als Array in properties.items und erstelle keine separaten Elemente für diese Einträge. Beziehungen dürfen nur zur Sammlung als Ganzem führen. Verwende höchstens 12 gut lesbare Einträge pro Sammlung; teile längere Listen in fachlich sinnvolle Sammlungen auf.
Nutze type "collection-group", wenn mehrere alternative einfache Sammlungen unter einer gemeinsamen Überschrift verwaltet werden sollen. Lege jede Alternative als eigenes Element mit type "collection" und parentId der Sammlungsgruppe an. properties.activeCollectionId der Gruppe verweist exakt auf eine dieser untergeordneten Sammlungen; nur sie wird anfangs angezeigt. Die items gehören jeweils an die untergeordnete einfache Sammlung, niemals an die Sammlungsgruppe.

ADMINISTRATIVE EBENEN
Prüfe ausdrücklich, welche administrativen Ebenen der Staat besitzt. Lege bei einem föderalen oder anderweitig mehrstufigen Aufbau alle für das Verständnis nötigen Ebenen vorab als groups an, zum Beispiel Zentralstaat, Gliedstaaten/Regionen und kommunale Ebene:
{ "id": "bund", "label": "Bundesebene", "kind": "level", "active": true, "x": 0, "y": 0, "w": 1, "h": 1 }
Ordne jedes institutionell zuordenbare Element mit groupId genau seiner administrativen Ebene zu. groups sind Ebenenfilter, keine sichtbaren Institutionen. Verwende parentGroupId nur für echte hierarchische Ebenenverschachtelung. Bei mehreren Ebenen erhält nur die nationale bzw. oberste Ebene active: true; alle nachgeordneten Ebenen beginnen mit active: false. Dadurch öffnet sich das importierte Modell übersichtlich und weitere Ebenen lassen sich gezielt zuschalten. Bei einem Zentralstaat genügt eine Gruppe für die nationale Ebene; ergänze nachgeordnete Ebenen nur, wenn sie für den dargestellten Staatsaufbau fachlich relevant sind.

BEZIEHUNGEN
Jede gerichtete Beziehung hat diese Form:
{
  "id": "eindeutige-beziehungs-id",
  "source": "id-des-ausgangselements",
  "target": "id-des-ziel-elements",
  "type": "wählt",
  "category": "Besetzung und Legitimation",
  "properties": {
    "sources": [
      { "law": "Name des Gesetzes oder der Verfassung", "citation": "Art./§ und Absatz", "url": "https://amtliche-quelle.example/...", "note": "Kurze Erklärung der Beziehung" }
    ]
  },
  "visual": {
    "sourcePort": { "side": "bottom", "t": 0.5 },
    "targetPort": { "side": "top", "t": 0.5 },
    "labelPosition": 0.5
  }
}
Die Richtung muss als lesbarer Satz funktionieren: [source] [type] [target]. Beispiel: "wahlberechtigte wählt parlament". side ist ausschließlich "left", "right", "top" oder "bottom"; t liegt zwischen 0 und 1. Lasse manualRoute weg, damit die App orthogonal und kollisionsfrei routet.

Zulässige Kategorien und bevorzugte type-Werte:
${relations}
Falls keine Standardformulierung die Beziehung korrekt beschreibt, darf type eine präzise eigene Formulierung mit höchstens 100 Zeichen enthalten; category muss dann trotzdem exakt eine der genannten Kategorien sein. Erfasse verschiedene semantische Beziehungsarten getrennt. Die App zeigt jeweils nur eine Beziehungsrubrik, daher müssen die Elemente unabhängig von der aktiven Rubrik verständlich bleiben.

LAYOUT
- Strukturiere das Canvas nach zwei klaren Achsen. Die vertikale Achse zeigt die administrative Hierarchie: nationale bzw. zentrale Ebene oben, darunter Gliedstaaten/Regionen und darunter die kommunale Ebene. Reserviere für jede Ebene ein eigenes horizontales Band und halte zwischen den Bändern mindestens 264 Einheiten Abstand. Elemente verschiedener Ebenen dürfen nicht ungeordnet vermischt werden.
- Die horizontale Achse zeigt innerhalb jedes Ebenenbandes die Gewaltenteilung. Ordne von links nach rechts grundsätzlich Legislative, Exekutive und Judikative in drei deutlich getrennten Spalten an. Empfohlene Spaltenzentren sind x=350 für Legislative, x=850 für Exekutive und x=1350 für Judikative. Kontroll-, Wahl- und sonstige unabhängige Organe kommen in eine zusätzliche Randspalte oder zwischen die fachlich passenden Spalten, ohne deren Lesbarkeit zu stören.
- Platziere Volk bzw. Wahlkörper zentral oberhalb oder unterhalb der drei Gewalten, sodass Wahl- und Legitimationsbeziehungen kurz und eindeutig geführt werden können. Eine Verfassung kann als übergeordnetes Element zentral oberhalb der Staatsorgane stehen. Entscheide die Richtung einheitlich für das ganze Diagramm.
- Innerhalb einer Spalte wird ebenfalls hierarchisch von oben nach unten gegliedert: übergeordnetes Organ, seine Teile oder Amtsträger, danach nachgeordnete Behörden und Ressorts. Nutze parentId nur, wenn eine echte Enthaltensein-Beziehung vorliegt; eine bloße Weisungs- oder Kontrollbeziehung wird als relation modelliert.
- Berechne die y-Positionen aus der Ebene: erste Ebene ungefähr ab y=250, nächste Ebene ungefähr ab y=950, weitere Ebene ungefähr ab y=1650. Innerhalb eines Ebenenbandes liegen über- und nachgeordnete Elemente mit mindestens 150 Einheiten vertikalem Abstand untereinander.
- Verwende verbindlich ein 24-Einheiten-Raster (zweifache Pfeilgröße) und ausschließlich positive Koordinaten. Alle x- und y-Positionen sowie width- und height-Werte müssen durch 24 teilbar sein. Die Abstände werden zwischen den Außenkanten der Elemente beurteilt, nicht nur zwischen ihren Mittelpunkten.
- Halte zwischen zwei gewöhnlichen, nicht verschachtelten Elementen ungefähr 72 bis 120 Einheiten freien horizontalen und 72 bis 96 Einheiten freien vertikalen Raum. Die Elemente dürfen weder aneinanderkleben noch ohne semantischen Grund weit auseinanderstehen.
- Eng zusammengehörige Elemente innerhalb desselben Organs oder derselben Hierarchiekette dürfen etwas näher stehen, benötigen aber mindestens 48 Einheiten sichtbaren Außenkantenabstand. Zwischen verschiedenen Gewalten sollen mindestens 144 Einheiten freie Gasse liegen; zwischen administrativen Ebenen mindestens 216 Einheiten.
- Vermeide Detailteppiche aus vielen gleichartigen Ministerien, Behörden oder Agenturen. Wenn deren individuelle Beziehungen nicht zum Lernziel gehören, bilde stattdessen eine Institutionssammlung mit type "collection", etwa "Bundesministerien" oder "weitere Bundesbehörden", und führe die konkreten Namen in properties.items auf.
- Plane entlang häufiger Beziehungen freie Pfeilkanäle von mindestens 48 Einheiten Breite ein. Zwischen Elementkante und einer parallel vorbeilaufenden Verbindungslinie muss mindestens 24 Einheiten Abstand bleiben. Reserviere an beschrifteten Beziehungen zusätzlich ungefähr 48 Einheiten freien Raum um die Beschriftung.
- Richte Geschwisterelemente mit einheitlichen Abständen aus. Unterschiede zwischen vergleichbaren Abständen sollen höchstens eine Rastereinheit von 24 betragen. Nutze größere Abstände gezielt nur zur Kennzeichnung eines neuen institutionellen Blocks, einer anderen Gewalt oder einer anderen administrativen Ebene.
- Halte um die gesamte Komposition einen Außenrand von mindestens 168 Einheiten frei. Ein Ebenenband soll kompakt bleiben: Seine zusammengehörigen Elemente sollen nicht weiter als ungefähr 696 Einheiten vertikal auseinanderliegen, sofern der Umfang keine zusätzliche Untergliederung verlangt.
- Elemente dürfen nicht überlappen. Plane ausreichend Raum für orthogonale Verbindungslinien und Beschriftungen ein.
- width liegt gewöhnlich zwischen 168 und 264 und ist stets ein Vielfaches von 24; bei langen Namen darf es in weiteren 24er-Schritten größer sein. height folgt derselben Regel.
- Prüfe vor der Ausgabe die Gesamtkomposition: administrative Ebenen müssen vertikal klar lesbar sein; Legislative, Exekutive und Judikative müssen horizontal wiederkehrende Spalten bilden; Institutionen derselben Art sollen möglichst auf gemeinsamen Fluchtlinien stehen. Verkleinere übergroße Leerräume und vergrößere zu enge Stellen, bis Gruppenzugehörigkeit und Trennung auf den ersten Blick erkennbar sind.

INHALTLICHER UMFANG
Berücksichtige mindestens: Staatsvolk bzw. Wahlberechtigte, Verfassungsordnung, Staatsoberhaupt, Parlament und gegebenenfalls seine Kammern, Regierung und Regierungsleitung, zentrale Gerichtsbarkeit bzw. Verfassungsgerichtsbarkeit, wichtige Kontrollorgane sowie bei föderalen Staaten die institutionell relevanten Gliedstaatsebenen. Nimm weitere Organe nur auf, wenn sie zum Verständnis des konkreten Systems beitragen. Dokumentiere die wichtigsten Funktionen der Elemente und die wichtigsten Beziehungen möglichst mit konkreten Verfassungs- oder Gesetzesquellen.

Prüfe vor der Ausgabe intern: gültiges JSON, eindeutige IDs, erlaubte Elementtypen, vorhandene Quell- und Ziel-IDs, exakte Kategorienamen, keine Selbstbeziehungen, numerische Positionen und keine Markdown-Zeichen außerhalb von JSON.`}
  function createGenerationPrompt(){const input=$('#generatorState'),state=input.value.trim();if(!state){input.setCustomValidity('Bitte gib einen Staat ein.');input.reportValidity();return}input.setCustomValidity('');$('#generatedPrompt').value=generationPrompt(state);$('#generatedPromptBlock').hidden=false;$('#promptCopyStatus').textContent='';requestAnimationFrame(()=>$('#generatedPrompt').focus())}
  async function copyGenerationPrompt(){const field=$('#generatedPrompt');if(!field.value)return;let copied=false;try{if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(field.value);copied=true}}catch{}if(!copied){field.focus();field.select();copied=document.execCommand('copy');field.setSelectionRange(0,0)}$('#promptCopyStatus').textContent=copied?'Prompt wurde kopiert.':'Kopieren war nicht möglich. Bitte den Text manuell markieren.'}
  function fit(){const b=exportBounds(),w=els.canvas.clientWidth,h=els.canvas.clientHeight;model.view=model.view||{};model.view.zoom=Math.max(.1,Math.min(1.6,Math.min(w/(b.r-b.l),h/(b.b-b.t))));model.view.panX=w/2-(b.l+b.r)/2*model.view.zoom;model.view.panY=h/2-(b.t+b.b)/2*model.view.zoom;reframeCanvas(true)}
  function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}

  // One geometry model drives hit testing, display and exported output.
  function hyphenateWord(word,max){if(word.length<=max)return[word];const parts=[],vowels='aeiouyäöüAEIOUYÄÖÜ';let rest=word;while(rest.length>max){let cut=-1;for(let i=Math.min(max-1,rest.length-1);i>=4;i--){if(vowels.includes(rest[i-1])&&!vowels.includes(rest[i])){cut=i;break}}if(cut<4)cut=Math.max(3,max-1);parts.push(rest.slice(0,cut)+'-');rest=rest.slice(cut)}parts.push(rest);return parts}
  function wrapLabel(text,width){const max=Math.max(8,Math.floor(width/8)),lines=[];for(const paragraph of String(text||'').split('\n')){let line='';for(const word of paragraph.split(/\s+/)){const pieces=hyphenateWord(word,max);if(pieces.length===1){if(line&&line.length+word.length+1>max){lines.push(line);line=''}line+=(line?' ':'')+word;continue}if(line){if(line.length+1+pieces[0].length<=max){line+=(line?' ':'')+pieces.shift()}else{lines.push(line);line=''}}pieces.forEach((piece,i)=>{if(i<pieces.length-1){lines.push(piece)}else{line=piece}})}lines.push(line)}return lines}
  function nodeDepth(n){let depth=0;while(n?.parentId&&depth<50){depth++;n=model.elements.find(x=>x.id===n.parentId)}return depth}
  function nodeBox(n){const p=nodePosition(n),size=nodeSize(n);return{l:p.x-size.w/2,r:p.x+size.w/2,t:p.y-size.h/2,b:p.y+size.h/2}}
  function connectionBox(n){return nodeBox(n)}
  function relationPorts(r){const s=model.elements.find(n=>n.id===r.source),t=model.elements.find(n=>n.id===r.target),a=nodePosition(s),b=nodePosition(t),horizontal=Math.abs(a.x-b.x)>Math.abs(a.y-b.y);const result={source:r.visual?.sourcePort||{side:horizontal?(a.x<b.x?'right':'left'):(a.y<b.y?'bottom':'top'),t:.5},target:r.visual?.targetPort||{side:horizontal?(a.x<b.x?'left':'right'):(a.y<b.y?'top':'bottom'),t:.5}};return{source:DiagramGeometry.snapPort(connectionBox(s),result.source),target:DiagramGeometry.snapPort(connectionBox(t),result.target)}}
  function renderEndpointHandles(){const layer=$('#endpointLayer');if(!layer)return;layer.innerHTML='';const r=selected?.kind==='edge'?model.relations.find(x=>x.id===selected.id):null;const ids=r?[r.source,r.target]:pending?model.elements.map(n=>n.id):selected?.kind==='node'?[selected.id]:[];layer.innerHTML=model.elements.filter(n=>ids.includes(n.id)&&nodeLevelActive(n)).map(n=>'<g class="port-markers active" data-node="'+n.id+'">'+DiagramGeometry.ports(nodeBox(n)).map(port=>{const p=DiagramGeometry.anchor(nodeBox(n),port);return '<circle class="port-marker '+(hoverPort?.id===n.id&&hoverPort.port.side===port.side&&hoverPort.port.t===port.t?'is-hot':'')+'" data-side="'+port.side+'" data-t="'+port.t+'" cx="'+p.x+'" cy="'+p.y+'" r="'+(5/currentZoom())+'"/>'}).join('')+'</g>').join('');layer.querySelectorAll('.port-marker').forEach(el=>{el.onpointerdown=e=>{if(e.button!==0)return;const group=el.closest('.port-markers');startConnect(e,group.dataset.node,{side:el.dataset.side,t:Number(el.dataset.t)})}});if(!r)return;const points=routeRelation(r),ports=relationPorts(r);layer.innerHTML+=points.slice(2,-1).map((p,i)=>{const a=points[i+1],horizontal=Math.abs(a.y-p.y)<.001;return '<line class="segment-handle" data-index="'+(i+1)+'" x1="'+a.x+'" y1="'+a.y+'" x2="'+p.x+'" y2="'+p.y+'" style="cursor:'+(horizontal?'ns-resize':'ew-resize')+'"><title>Abschnitt quer verschieben</title></line>'}).join('');const visibleCorners=points.slice(1,-1).map((p,i)=>({p,index:i+1})).filter(({p,index})=>{const a=points[index-1],b=points[index+1],collinear=(Math.abs(a.x-p.x)<.001&&Math.abs(p.x-b.x)<.001)||(Math.abs(a.y-p.y)<.001&&Math.abs(p.y-b.y)<.001),temporary=preservedPointRelationId===r.id;return !collinear||temporary});layer.innerHTML+=visibleCorners.map(({p,index})=>{const temporary=preservedPointRelationId===r.id;return '<circle class="corner-handle '+(!temporary&&(index===1||index===points.length-2)?'protected':'')+'" data-index="'+index+'" cx="'+p.x+'" cy="'+p.y+'" r="'+(5/currentZoom())+'"><title>Ecke ziehen · Rechtsklick entfernt den Punkt</title></circle>'}).join('');layer.innerHTML+=['source','target'].map(end=>{const p=DiagramGeometry.anchor(nodeBox(model.elements.find(n=>n.id===r[end])),ports[end]);return '<circle class="endpoint-handle" data-end="'+end+'" cx="'+p.x+'" cy="'+p.y+'" r="'+(6/currentZoom())+'"><title>Am nächsten Rasterpunkt einrasten</title></circle>'}).join('');layer.querySelectorAll('.corner-handle').forEach(el=>{el.onpointerdown=e=>{if(e.button!==0)return;e.preventDefault();e.stopPropagation();drag={kind:'corner',id:r.id,index:Number(el.dataset.index),points,terminal:el.classList.contains('protected')&&Number(el.dataset.index)===points.length-2,originalVisual:JSON.parse(JSON.stringify(r.visual||{})),invalid:false};els.canvas.setPointerCapture(e.pointerId)};el.oncontextmenu=e=>{e.preventDefault();e.stopPropagation();if(el.classList.contains('protected'))return;r.visual=r.visual||{};r.visual.manualRoute=DiagramGeometry.simplify(points.filter((_,i)=>i!==Number(el.dataset.index)));preservedPointRelationId=null;preservedPoint=null;r.visual.freeRoute=true;hoverPort=null;renderEdges();save()}});layer.querySelectorAll('.endpoint-handle').forEach(el=>el.onpointerdown=e=>{if(e.button!==0)return;e.preventDefault();e.stopPropagation();drag={kind:'endpoint',id:r.id,end:el.dataset.end};els.canvas.setPointerCapture(e.pointerId)});layer.querySelectorAll('.segment-handle').forEach(el=>{el.ondblclick=e=>addRoutePoint(e,r.id);el.onpointerdown=e=>{if(e.button!==0)return;if(handleRoutePress(e,r.id))return;e.preventDefault();e.stopPropagation();const index=Number(el.dataset.index),axis=Math.abs(points[index].y-points[index+1].y)<.001?'y':'x';drag={kind:'segment',id:r.id,index,points,start:point(e)[axis],axis,originalVisual:JSON.parse(JSON.stringify(r.visual||{})),invalid:false};els.canvas.setPointerCapture(e.pointerId)}})}
  function syncPrecisionInspector(){const field=$('#nodeWidthField');if(!field)return;const n=selected?.kind==='node'?model.elements.find(x=>x.id===selected.id):null;field.hidden=!n;if(n)$('#nodeWidth').value=n.visual.width||180}
  let undoStack=[],redoStack=[],savedSnapshot=null;
  function remember(){const next=JSON.stringify(model);if(savedSnapshot!==null&&next!==savedSnapshot){undoStack.push(savedSnapshot);if(undoStack.length>70)undoStack.shift();redoStack=[]}savedSnapshot=next}
  function historyStep(redo){const from=redo?redoStack:undoStack,to=redo?undoStack:redoStack;if(!from.length)return;to.push(JSON.stringify(model));model=JSON.parse(from.pop());savedSnapshot=JSON.stringify(model);selected=null;pending=null;drag=null;$('#modelTitle').value=model.title;setMode(model.mode,false);applyView();render();save()}
  function validateModel(data){
    if(data?.version!==1||!Array.isArray(data.elements)||!Array.isArray(data.relations))throw Error('Keine gültige Modelldatei.');
    const copy=JSON.parse(JSON.stringify(data));copy.groups=copy.groups||[];copy.branches=normalizedBranches(copy.branches);copy.view=copy.view||{};copy.title=String(copy.title||'Mein Staatsmodell');
    if(copy.elements.length>200||copy.relations.length>500)throw Error('Das Modell ist zu groß (maximal 200 Elemente / 500 Beziehungen).');
    const ids=new Set();for(const n of [...copy.elements,...copy.groups,...copy.relations]){if(typeof n.id!=='string'||!/^[-\w]+$/.test(n.id)||ids.has(n.id))throw Error('Ungültige oder doppelte Kennung.');ids.add(n.id)}
    const ni=new Set(copy.elements.map(n=>n.id)),gi=new Set(copy.groups.map(g=>g.id));
    for(const n of copy.elements){n.type=normalizeElementType(n.type);n.label=String(n.label||'');n.properties=n.properties||{};if(n.type==='collection')n.properties.items=collectionEntries(n).map(({label,short})=>short?{label,short}:label);n.visual=n.visual||{};if(!Number.isFinite(n.visual.x)||!Number.isFinite(n.visual.y))throw Error('Ungültige Elementposition.');if(n.parentId&&!ni.has(n.parentId))throw Error('Übergeordnetes Element fehlt.');if(n.groupId&&!gi.has(n.groupId))n.groupId=''}
    for(const g of copy.groups){if(!['x','y','w','h'].every(k=>Number.isFinite(g[k]))||g.w<1||g.h<1)throw Error('Ungültige Bereichsgröße.');if(g.parentGroupId&&!gi.has(g.parentGroupId))throw Error('Übergeordneter Bereich fehlt.')}
    for(const [items,key] of [[copy.elements,'parentId'],[copy.groups,'parentGroupId']])for(const item of items){const seen=new Set([item.id]);let p=item[key];while(p){if(seen.has(p))throw Error('Kreisförmige Verschachtelung.');seen.add(p);p=items.find(n=>n.id===p)?.[key]}}
    for(const r of copy.relations){
      r.type=String(r.type||'').trim();const standard=Object.values(RELATIONS).flat().includes(r.type),custom=RELATIONS[r.category]&&r.type.length>0&&r.type.length<=100;
      if(!ni.has(r.source)||!ni.has(r.target)||r.source===r.target||(!standard&&!custom))throw Error('Ungültige Beziehung.');
      r.visual=r.visual||{};if(r.visual.manualRoute&&(!Array.isArray(r.visual.manualRoute)||r.visual.manualRoute.length<2||r.visual.manualRoute.length>200||r.visual.manualRoute.some(p=>!Number.isFinite(p.x)||!Number.isFinite(p.y))))throw Error('Ungültiger manueller Pfeilverlauf.');r.properties=r.properties||{};
      for(const end of ['sourcePort','targetPort']){const p=r.visual[end];if(p&&(!['left','right','top','bottom'].includes(p.side)||!Number.isFinite(p.t)||p.t<0||p.t>1))throw Error('Ungültiger Pfeilanschluss.')}
    }
    return copy
  }
  function createFramework(){if((model.elements.length||model.groups.length)&&!confirm('Das aktuelle Modell durch vorbereitete staatliche Ebenen ersetzen? Vorher bei Bedarf exportieren.'))return;const mode=model.mode;model=blank();model.mode=mode;model.title='Staatsaufbau';['Zentralstaat','Gliedstaaten / Regionen','Kommunale Ebene'].forEach(label=>model.groups.push({id:uid('group'),label,kind:'level',active:true,x:0,y:0,w:1,h:1}));selected=null;$('#modelTitle').value=model.title;render();centerElementsView();save()}
  let hoverPort=null,hoverNodeId=null,suppressNodeClickId=null;
  function findHoverPort(p){let best=null;const candidates=pending?model.elements:selected?.kind==='node'?model.elements.filter(n=>n.id===selected.id):[];for(const n of candidates){if(!nodeLevelActive(n))continue;const box=connectionBox(n),port=DiagramGeometry.snapPort(box,DiagramGeometry.nearestPort(box,p)),at=DiagramGeometry.anchor(box,port),distance=Math.hypot(at.x-p.x,at.y-p.y);if(distance<16/currentZoom()&&(!best||distance<best.distance))best={id:n.id,port,point:at,distance}}return best}
  let reconnectDrag=null,draggedLevelId=null,levelDropTargetId=null,levelDropAfter=false;
  function commitLevelReorder(){if(!draggedLevelId||!levelDropTargetId||draggedLevelId===levelDropTargetId)return false;const from=model.groups.findIndex(item=>item.id===draggedLevelId);if(from<0)return false;const [moved]=model.groups.splice(from,1),target=model.groups.findIndex(item=>item.id===levelDropTargetId);model.groups.splice(target+(levelDropAfter?1:0),0,moved);draggedLevelId=null;levelDropTargetId=null;renderGroups();save();return true}
  function reconnectCandidate(p,relation,end){let best=null;const otherId=relation[end==='source'?'target':'source'];for(const n of model.elements){if(n.id===otherId||!nodeVisible(n)||!nodeLevelActive(n))continue;const box=connectionBox(n),port=DiagramGeometry.snapPort(box,DiagramGeometry.nearestPort(box,p)),at=DiagramGeometry.anchor(box,port),distance=Math.hypot(at.x-p.x,at.y-p.y);if(distance<22/currentZoom()&&(!best||distance<best.distance))best={id:n.id,port,point:at,distance}}return best}
  function renderReconnectTargets(candidate){const layer=$('#endpointLayer');if(!layer||!reconnectDrag)return;const relation=model.relations.find(r=>r.id===reconnectDrag.id),otherId=relation?.[reconnectDrag.end==='source'?'target':'source'];for(const n of model.elements){if(n.id===otherId||!nodeVisible(n)||!nodeLevelActive(n))continue;const group=document.createElementNS('http://www.w3.org/2000/svg','g');group.classList.add('port-markers','active');group.dataset.node=n.id;group.innerHTML=DiagramGeometry.ports(nodeBox(n)).map(port=>{const p=DiagramGeometry.anchor(nodeBox(n),port),hot=candidate?.id===n.id&&candidate.port.side===port.side&&candidate.port.t===port.t;return '<circle class="port-marker '+(hot?'is-hot':'')+'" cx="'+p.x+'" cy="'+p.y+'" r="'+(5/currentZoom())+'"/>'}).join('');layer.append(group)}}
  function setupEnhancements(){
    $('#editCollectionGroup').onclick=openCollectionGroupEditor;
    const framework=document.createElement('button');framework.id='frameworkButton';framework.className='secondary wide advanced-only';framework.hidden=model.mode!=='advanced';framework.textContent='Ebenenstruktur vorbereiten';framework.onclick=createFramework;$('.palette').append(framework);
    $('.palette .hint').textContent='Klicken zum Kennenlernen. Auf die Fläche ziehen zum Platzieren.';
    $('#connectionHint').hidden=true;
    const overlay=document.createElementNS('http://www.w3.org/2000/svg','svg');overlay.id='endpointOverlay';overlay.innerHTML='<g id="endpointLayer"></g>';els.viewport.append(overlay);
    els.edges.addEventListener('click',e=>{const marker=e.target.closest?.('.edge-source-mark');if(!marker)return;e.preventDefault();e.stopPropagation();showSourceInfo(marker.dataset.id)});
    const width=document.createElement('label');width.id='nodeWidthField';width.innerHTML='Breite des Elements · 24-px-Raster<input id="nodeWidth" type="range" min="168" max="696" step="24">';els.nodeForm.insertBefore(width,els.nodeForm.querySelector('.advanced-fields'));$('#nodeWidth').oninput=e=>{e.stopPropagation();const n=getSelected();if(selected?.kind!=='node')return;n.visual.width=snapGrid(Number(e.target.value));renderNodes();renderEdges();save()};
    els.canvas.addEventListener('pointerdown',e=>{if(e.button===0&&!(e.target.closest('.node,.group,.collection-menu,.level-filter,.branch-filter,.relation-layer-filter,.edge-label,.edge-source-mark,.edge-hit,.endpoint-handle,.segment-handle,.corner-handle,.port-marker'))){if(preservedPointRelationId){normalizeRelation(model.relations.find(r=>r.id===preservedPointRelationId));preservedPointRelationId=null;preservedPoint=null}pending=null;selected=null;render();save()}},true);
    document.addEventListener('pointerdown',e=>{if(openCollectionId&&!e.target.closest('.collection-menu,.collection-toggle')){openCollectionId=null;renderNodes();renderCollectionMenu()}});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&openCollectionId){openCollectionId=null;renderNodes();renderCollectionMenu()}});
    // Capture stays on the canvas even when SVG handles are redrawn during a drag.
    els.canvas.addEventListener('pointermove',()=>{if(drag?.kind!=='edge-label')return;const label=els.edges.querySelector(`.edge-label[data-id="${drag.id}"]`),bg=els.edges.querySelector(`.edge-label-bg[data-id="${drag.id}"]`);if(!label||!bg)return;const x=Number(label.getAttribute('x')),y=Number(label.getAttribute('y'));bg.setAttribute('x',x-Number(bg.getAttribute('width'))/2);bg.setAttribute('y',y-15)});
    els.filters.addEventListener('dragstart',e=>{const level=e.target.closest?.('.level-filter');if(level){draggedLevelId=level.dataset.id;levelDropTargetId=null}},true);
    els.filters.addEventListener('dragover',e=>{const level=e.target.closest?.('.level-filter');if(!level||!draggedLevelId)return;e.preventDefault();levelDropTargetId=level.dataset.id;levelDropAfter=e.clientY>=level.getBoundingClientRect().top+level.offsetHeight/2},true);
    els.filters.addEventListener('drop',e=>{if(!draggedLevelId)return;e.preventDefault();e.stopImmediatePropagation();commitLevelReorder()},true);
    els.filters.addEventListener('dragend',()=>{if(!commitLevelReorder()){draggedLevelId=null;levelDropTargetId=null}},true);
    els.canvas.addEventListener('pointerdown',e=>{const handle=e.target.closest?.('.endpoint-handle');if(e.button!==0||!handle)return;const relation=selected?.kind==='edge'?model.relations.find(r=>r.id===selected.id):null;if(!relation)return;reconnectDrag={id:relation.id,end:handle.dataset.end,originalNodeId:relation[handle.dataset.end],originalVisual:JSON.parse(JSON.stringify(relation.visual||{})),candidate:null};requestAnimationFrame(()=>renderReconnectTargets(null))},true);
    els.canvas.addEventListener('pointermove',e=>{if(!reconnectDrag)return;e.preventDefault();e.stopImmediatePropagation();const relation=model.relations.find(r=>r.id===reconnectDrag.id),candidate=reconnectCandidate(point(e),relation,reconnectDrag.end);relation[reconnectDrag.end]=candidate?.id||reconnectDrag.originalNodeId;relation.visual=JSON.parse(JSON.stringify(reconnectDrag.originalVisual));if(candidate)relation.visual[reconnectDrag.end+'Port']=candidate.port;reconnectDrag.candidate=candidate;renderEdges();renderReconnectTargets(candidate)},true);
    const finishReconnect=()=>{if(!reconnectDrag)return;const state=reconnectDrag,relation=model.relations.find(r=>r.id===state.id);if(!state.candidate){relation[state.end]=state.originalNodeId;relation.visual=state.originalVisual}else normalizeRelation(relation);reconnectDrag=null;drag=null;hoverPort=null;reframeCanvas(false);renderEdges();renderInspector();save()};
    els.canvas.addEventListener('pointerup',finishReconnect,true);els.canvas.addEventListener('pointercancel',finishReconnect,true);
    document.addEventListener('keydown',e=>{if(e.target.matches('input,textarea,select')||document.querySelector('dialog[open]'))return;if((e.ctrlKey||e.metaKey)&&['z','y'].includes(e.key.toLowerCase())){e.preventDefault();historyStep(e.key.toLowerCase()==='y'||e.shiftKey);return}const direction={ArrowLeft:[1,0],ArrowRight:[-1,0],ArrowUp:[0,1],ArrowDown:[0,-1]}[e.key];if(direction){e.preventDefault();const step=e.shiftKey?144:48;panViewBy(direction[0]*step,direction[1]*step)}});
    $('#relationDialog').addEventListener('close',()=>{pending=null;renderEdges()});
    for(const id of ['exportTheme','exportTitle','exportPaper','exportOrientation'])$('#'+id).addEventListener('change',()=>{const warnings=exportWarnings(),box=$('#exportValidation');box.classList.toggle('has-warnings',warnings.length>0);box.textContent=warnings.length?warnings.join(' '):'Das Modell ist für den Export bereit.'});
    remember();syncPrecisionInspector();
  }

  TYPE_INFO.constitution=['Eine Verfassung oder Grundordnung legt fest, wie staatliche Herrschaft organisiert und begrenzt wird. Sie ist eine Rechtsgrundlage, kein handelndes Organ.','Verbinde die Grundordnung etwa mit „begründet Kompetenzen von“. Wer ihre Einhaltung kontrolliert, wird durch eine eigene Beziehung zum zuständigen Gericht dargestellt.','Verfassung, Grundgesetz, historische Grundordnung'];
  TYPE_INFO.collection=['Fasst mehrere Institutionen platzsparend in einem aufklappbaren Diagrammelement zusammen.','Nutze die Sammlung, wenn Lernende die einzelnen Institutionen bei Bedarf nachschlagen sollen, diese aber noch keine eigenen Beziehungen oder Eigenschaften benötigen. Beziehungen werden zunächst ausschließlich mit der Sammlung als Ganzem gezeichnet.','Bundesministerien, nachgeordnete Behörden, weitere Bundesagenturen'];
  RELATIONS['Rechtliche Grundlagen']=['begründet Kompetenzen von','bindet','prüft Vereinbarkeit mit'];
  init();
  setupEnhancements();
  const workspace=document.querySelector('.workspace');
  for(const [side,selector,buttonId,label] of [['left','.palette','togglePalette','Elemente'],['right','.inspector','toggleInspector','Auswahl']]){
    const panel=workspace.querySelector(selector),button=document.getElementById(buttonId);
    panel.id=side==='left'?'palettePanel':'inspectorPanel';
    button.onclick=()=>{
      const collapsed=workspace.classList.toggle(`hide-${side}-panel`);
      panel.inert=collapsed;panel.setAttribute('aria-hidden',String(collapsed));
      button.setAttribute('aria-expanded',String(!collapsed));
      const action=`${label} ${collapsed?'einblenden':'ausblenden'}`;
      button.title=action;button.setAttribute('aria-label',action);
      button.textContent=side==='left'?(collapsed?'›':'‹'):(collapsed?'‹':'›');
    };
  }
})();
